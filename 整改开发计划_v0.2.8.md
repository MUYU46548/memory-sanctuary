# 记忆圣所 · 整改开发计划（v0.2.8）

> **执行前必读**：本计划分三个包，按风险从低到高排列。每个任务含「现象/影响/修复/验证」四要素，可直接转给 OpenSquilla 执行。
> 
> **存档策略**：老存档不作迁移不作重算，全炸无所谓。

---

## 包 A：机械修复（零决策，~15 分钟）

> 性质：纯代码/数据修正，无需设计判断。全部完成后跑一遍语法检查即可。

### T-A1：成就条件值类型错误

**现象**：`data/achievements.json` 中 `all_guardians_intimate` 的 `condition.value` 为数字 `4`，但代码 `js/game.js:3002-3006` 用 `tiers[c.value]` 查表——`tiers[4]` 是 `undefined`，静默回退为 `3`（friendly），导致该成就实际在"友好"即可解锁，而非"亲密"。

**影响**：成就门槛虚低，玩家未达成设计意图的"亲密"条件即可解锁。

**修复**：
1. `data/achievements.json`：`all_guardians_intimate` 的 `condition.value` 从 `4` 改为 `"intimate"`
2. `js/game.js:3002-3006`：将 `tiers[c.value] || 3` 改为 fail-fast 写法：
   ```js
   const targetTier = tiers[c.value];
   if (targetTier === undefined) throw new Error(`Unknown mood tier: ${c.value} in achievement ${ach.id}`);
   ```
3. 全局搜索 `|| 3`、`|| 0`、`|| 'default'` 等同款兜底写法，同类全部改为 throw 或显式校验。

**验证**：
```bash
# 语法检查
for f in js/*.js; do node -c "$f"; done

# 成就解锁路径冒烟：加载全部 JSON + 跑一遍 checkAchievements，确认无 throw
node -e "
const achs=require('./data/achievements.json').achievements;
const tiers={hostile:0,cold:1,neutral:2,friendly:3,intimate:4};
achs.filter(a=>a.condition?.type==='all_guardians_mood').forEach(a=>{
  if(tiers[a.condition.value]===undefined) console.log('STILL BROKEN:', a.id, a.condition.value);
  else console.log('OK:', a.id, '->', a.condition.value);
});
"
```

---

### T-A2：删除死代码 `startGameAfterLoad`

**现象**：`js/game-save.js:968` 的 `startGameAfterLoad()` 函数在整个项目中**零调用者**（grep 结果只有定义处本身）。该函数内部调用 `loadGame(slot)` 但缺少 `renderAll()`，若被误用会导致 UI 不刷新。

**影响**：死代码占用认知负荷，且存在潜在 bug 风险。

**修复**：删除 `js/game-save.js:968-984` 的整个 `startGameAfterLoad` 函数。

**验证**：
```bash
# 确认无调用者
grep -rn "startGameAfterLoad" js/
# 预期输出：无结果

# 语法检查
node -c js/game-save.js
```

---

## 包 B：设计决策（需要人表态）

> 性质：涉及游戏设计判断，不能由执行者自行决定。需先完成「结局触发条件关系分析」，再定修法。

### T-B1：结局触发条件关系分析（前置分析任务）

**现象**：经济模拟显示 skilled bot 100% 收敛于 `finale_whisper_keeper`，但**无法区分**是"数值缺陷"还是"测量盲区"（bot 只能优化资源分配，测不了叙事分支）。

**影响**：如果盲目调整资源曲线，可能破坏平衡却治标不治本。

**分析需求**：
1. 列出全部结局的触发条件（资源阈值、叙事标志、守护者状态等）
2. 判断 `finale_whisper_keeper` 的条件是否是其他结局条件的**超集**（即满足 whisper_keeper 的条件必然满足其他结局）
3. 判断结局之间是否**互斥**（同一周目能否同时满足多个结局条件）
4. 判断其他结局的触发路径是否被**数值墙**挡死（例如需要某资源 > 200 但上限只有 150）

**交付物**：一份结局条件关系矩阵表，标注每个结局的触发条件、互斥关系、可达性瓶颈。

**验证**：人工审阅矩阵表，确认逻辑自洽。

---

### T-B2：基于分析结果的结局平衡调整（依赖 T-B1）

**现象**：待定（取决于 T-B1 结论）。

**修复**：待定。可能方向包括：
- 若为超集问题：拆分结局条件，增加互斥分支
- 若为数值墙：调整资源上限或结局阈值
- 若为 bot 测量盲区：无需修改数值，补充叙事引导

**验证**：重新跑平衡模拟，对比调整前的结局分布。

---

## 包 C：测量修正（下轮模拟方法论）

> 性质：不修改游戏代码，只修正模拟器的测量方法。

### T-C1：补报样本量 n 和置信区间

**现象**：当前模拟报告只报百分比，缺少原始样本量 n，无法判断置信度。

**修复**：在 `balance-sim-v2.js` 的报告输出中增加：
- 每策略的模拟局数 n
- 关键指标（归档率、崩溃率）的 95% 置信区间

**验证**：重新跑模拟，确认报告包含 n 和置信区间。

---

### T-C2：补充"直觉玩家"策略档

**现象**：当前策略档只有 random/greedy/skilled 三档，缺少"有经验但非最优的人类玩家"基线。

**修复**：在 `balance-sim-v2.js` 中新增 `intuitive` 策略：
- 优先归档高价值条目（主题契合 + 存储室奖励）
- 资源低于阈值时停止归档、优先勘探
- 不使用应急协议（除非崩溃边缘）
- 模拟人类"够用就好"的决策风格

**验证**：新策略的归档率应介于 greedy 和 skilled 之间，崩溃率应低于 greedy。

---

### T-C3：expiry 策略异常单独归因

**现象**：expiry 策略（理性优先吃快过期条目）的崩溃率（59.2%）比 random（53.1%）还高，违反直觉。

**修复**：在模拟器中增加 expiry 策略的**决策日志**，输出每局的：
- 每周的资源变化曲线
- 归档/勘探/跳过的决策序列
- 崩溃前的最后 5 周状态

**验证**：通过决策日志定位崩溃根因（是腐坏机制设计问题还是策略本身缺陷）。

---

## 验收标准（Acceptance Criteria）

### 包 A 验收
- [ ] `node -c js/*.js` 全部通过
- [ ] `all_guardians_intimate` 的 `condition.value` 为字符串 `"intimate"`
- [ ] `tiers[c.value]` 的 fail-fast 写法已落地，无 `|| 3` 同款兜底残留
- [ ] `startGameAfterLoad` 已从代码库中移除
- [ ] 成就解锁路径冒烟测试通过，无 throw

### 包 B 验收
- [ ] 结局条件关系矩阵表已完成
- [ ] 矩阵表经人工审阅，逻辑自洽
- [ ] 基于分析结果的平衡调整方案已确认（"可接受"）
- [ ] 调整后模拟结果符合预期

### 包 C 验收
- [ ] 模拟报告包含样本量 n 和置信区间
- [ ] `intuitive` 策略已添加，数据符合预期
- [ ] expiry 策略决策日志已输出，根因已定位

---

## 执行顺序建议

```
T-A1 → T-A2 → 验收包A → T-B1 → 人工审阅 → T-B2 → 验收包B → T-C1/C2/C3 → 验收包C
```

包 A 和包 C 可并行执行（互不依赖）。包 B 必须串行，且 T-B1 完成后需人工确认才能进 T-B2。

---

## 不在本计划内的项目

| 项目 | 排除原因 |
|------|----------|
| vn.js / game-emergency.js 硬编码 hex → CSS 变量 | 有"CSS 变量读取时机"的坑（init 读一次缓存 = 主题切换不生效），值得单独一轮 |
| 崩溃率/饥饿率全局调整 | 短局重来成本低，先别动资源曲线；等人肉测试反馈后再定 |
| 老存档迁移/重算 | 用户明确：老存档不作迁移不作重算 |

---

*计划版本：v1.0 | 生成日期：2026-09-06 | 配套检查报告：游戏全面检查报告*
