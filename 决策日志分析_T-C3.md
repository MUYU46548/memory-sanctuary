# expiry 策略崩溃归因结论（T-C3 交付物）

> 依据：`balance-trace-expiry.txt`（10 局完整决策日志，含每周资源曲线 + 决策序列 + 崩溃前最后 5 周）
> 配套：`js/balance-sim-v2.js --trace expiry` 可复现
> 生成日期：2026-09-06

## 一、现象

expiry 策略（理性优先吃快过期条目）崩溃率 **59.2%**，比 random（53.1%）还高 6.1pct，违反直觉。
trace 抽样 10 局：collapse=5 · starvation=1 · 成功(whisper_keeper)=4，与全量 59.2% 吻合。

## 二、根因链（trace 证据）

**崩溃触发机制**（`balance-sim-v2.js` onTimeAdvanced 第 13 步）：能量/介质/环境 **≥2 项同时归零 → collapse**。

以崩溃局 #1（seed=1000，week=41 崩溃）为例：

```
w39: 能量=6.6  介质=19.8  环境=2.2  腐败=51  [batch×3]   ← 资源已见底仍批量吃急迫条目
w39: 能量=3.6  介质=16.8  环境=2.2  腐败=51  [quick]      ← 继续吃
w40: 能量=0    介质=11.5  环境=0.9  腐败=49  [archive]    ← 能量打穿归零
w41: 能量=0    介质=10.1  环境=0    腐败=47  [explore]    ← 环境归零 → 双零 → collapse
```

完整因果链：

1. **决策无视成本**：expiry 选条目只看 `expiresAfter - week`（谁最快过期吃谁），**完全不看 energy/media 成本**。资源低水位时仍优先吃高成本急迫条目。
2. **资源加速枯竭**：高成本归档 → 能量/介质快速见底 → 触发应急协议（`energy_extract` 用环境换能量 / `media_recycle` 用能量换介质）。
3. **腐败惩罚螺旋**：应急累积腐败 → 腐坏惩罚放大周衰减（`getWeeklyDecay` 随腐败上浮）→ 资源入不敷出加剧。
4. **环境抽干 → 双零崩溃**：末期环境被 `energy_extract` 抽到 0，能量也归 0 → 双零 → collapse。
5. **雪上加霜**：环境=0 时所有条目过期周**减半**（`getEffectiveExpiryWeeks`）→ 大量条目永久消失，完成度锁死。

## 三、关键对照实验（排除其他假说）

| 假说 | 验证 | 结论 |
|---|---|---|
| 应急次数过多 | 崩溃局平均 7.6 次 vs 成功局 7.8 次 | **排除**（无差异） |
| 腐败峰值过高 | 崩溃局峰值 57-64 vs 成功局 29-84（局8/9 峰值 82-84 仍成功） | **排除**（成功局峰值更高也能活） |
| 决策无视成本（急迫>可持续） | trace 显示低水位仍吃急迫条目 | **证实**（核心） |
| 腐坏机制本身有 bug | random 同机制崩溃率仅 53.1%（random 的 `getAffordableArchives` 天然过滤"买得起"的低成本条目） | 机制逻辑自洽，**放大器而非病因** |

## 四、定性结论

- **策略缺陷为主**：expiry 的"纯急迫优先"在资源稀缺时制造死亡螺旋——把急迫性置于可持续性之上，是非理性理性。
- **腐坏机制是放大器**：双零崩溃 + 腐败惩罚 + 环境归零加速过期，三者共同把"资源管理失误"放大成"整局崩溃"，但机制本身无逻辑 bug。
- **随机对照**：random 的随机选择 + 可负担过滤，期望成本低，反而更稳（53.1% 崩溃率 vs expiry 59.2%）。

## 五、处置建议

1. **游戏侧无需改动**：expiry 是模拟器测试策略，不是玩家可选行为；真实玩家不会只吃快过期条目。腐坏机制对"无脑急迫"的惩罚是合理的。
2. **模拟器侧（可选）**：若希望 expiry 作为"过期管理流派"参考基线，应加成本约束（如急迫条目成本 < 当前资源 50% 才吃），否则该档仅作为"负面教材"保留。
3. **后续平衡参考**：崩溃的共性前兆是**环境归零**（所有崩溃局最后 5 周环境 ≤2.2）——环境是比能量/介质更关键的衰竭信号，未来若调腐坏数值可优先关注环境的衰减/恢复曲线。

---

*验证命令：`node js/balance-sim-v2.js --trace expiry` → `balance-trace-expiry.txt`*
