# 记忆圣所 开发日志

> 项目名称：记忆圣所 (Memory Sanctuary / Nar-Sil-Veth)  
> 技术栈：HTML5 + CSS3 + Canvas 2D + Vanilla JavaScript  
> 目标平台：浏览器（桌面与平板）  
> 端口：8099  

注：本日志内容过长，请将后续内容写至新日志`开发日志2.md`。

---

## 项目概述

玩家扮演"静默之翼"守护者，在行星级灾难中管理一座深埋地下的记忆圣所。时间有限、资源递减，玩家必须在终结到来前决定保存文明的哪些碎片。每一段被归档的记忆都揭示萨拉达斯文明的一个切面——而玩家的选择决定了后世"看到"的是怎样的文明。

**核心循环**：发现文明碎片 → 分配到十二间存储室 → 消耗能源录入 → 维护圣所环境 → 应对突发事件 → 直至最终封存

---

## 当前状态

**版本**：v1.5.1 DLC 框架版
**代码规模**：~15,000+ 行（新增 DLC 模块系统 + 标题画面模式切换）
**验证通过**：JS 语法 ✓、HTTP 200 ✓、标题画面 DLC 按钮渲染 ✓

---

### 2026-08-23 — DLC 框架搭建

**目标**：预留 DLC 接口，实现标题画面模式切换 UI

#### 新增文件

| 文件 | 说明 |
|------|------|
| `js/dlc.js` | DLC 模块注册表、状态管理、数据隔离接口 |
| `data/dlc/ascension/` | 升天之仪 DLC 数据目录（待填充） |
| `data/dlc/greenOrb/` | 绿色缤球号 DLC 数据目录（待填充） |

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 修改 | 标题画面新增"选择模式"按钮 + DLC 选择面板 |
| `css/main.css` | 修改 | DLC 按钮样式 + 选择面板样式（双主题适配） |
| `js/main.js` | 修改 | DLC 按钮事件 + 面板渲染 + `showTitleScreen` 联动 |
| `js/game-save.js` | 修改 | 存档版本升至 2，新增 `modules` 字段 |

#### 预留接口清单

| 接口 | 说明 |
|------|------|
| `DLC_MODULES` | 模块注册表（sanctuary/ascension/greenOrb） |
| `isModuleUnlocked(id)` | 解锁判定（基于 NG+ 周目数） |
| `switchModule(id)` | 切换当前激活模块 |
| `loadModuleData(id)` | 异步加载 DLC 数据文件 |
| `getModuleState(id)` | 获取模块存档状态 |
| `saveModuleState(id, state)` | 保存模块状态 |
| `createDefaultModuleState(id)` | 创建模块默认状态 |
| `getCrossModuleBonuses(id)` | 跨模块联动奖励计算 |
| `MemorySanctuary.moduleData` | DLC 数据缓存 |
| `MemorySanctuary.state.modules` | 存档中的 DLC 状态隔离区 |

#### 存档结构变更

```js
// v2 存档新增字段
saveData.state.modules = {
    ascension: { unlocked, offerings, window, morale, narrativeFlags, completedItems },
    greenOrb:  { unlocked, scanEnergy, compute, license, recoveredFragments, narrativeFlags }
}
```

#### 待办事项

- [ ] 升天之仪核心玩法实现（极端取舍机制）
- [ ] 绿色缤球号核心玩法实现（拼图式考古）
- [ ] DLC 专属数据文件填充（ascension_archives.json 等）
- [ ] DLC 专属 VN 剧本（章节过渡 + 结局演出）
- [ ] DLC 专属 Canvas 渲染（发射器/全息投影）
- [ ] 跨模块联动逻辑实现（主游戏完成度影响 DLC 内容）

---

## 开发进度

### 2026-07-28 ~ 2026-07-29 — 9个阶段全部完成

| 阶段 | 日期 | 核心内容 | 验证 |
|------|------|----------|------|
| MVP-0 ~ MVP扩展 | 07-28 | 最小循环 + 12存储室 + 63条目 + 5守护者 | 59项 |
| 模块A+B ~ 中期 | 07-28 | 日志 + Canvas + 守护者互动 + 引导 | 26项 |
| 第二阶段 | 07-28 | 存档 + NG+ + 标题画面 | 14项 |
| 第三阶段（路线A） | 07-29 | 生存压力 + 衰竭机制 + 跳过回合 | 30项 |
| 第四阶段（路线B） | 07-29 | 线索链 + 图谱 + 画像 + 7隐藏结局 | 65项 |
| 第五阶段（UX） | 07-29 | 5项体验优化 | 25项 |
| 第六阶段（完整性） | 07-29 | 失败条件 + 周数上限 + 事件反馈 + 归档确认 | 30项 |
| Bug修复 | 07-29 | 确认弹窗 + 困局横幅 + 返回标题 + 衰减加速 | 25项 |
| 子系统C | 07-29 | 地表勘探系统 + 底部按钮视觉优化 | 73项 |
| 子系统D | 07-29 | 紧急归档 + 终章对话 + 维护项目 + 终局倒计时 | 64项 |

**累计验证**：300+ 项 ad-hoc 验证全部通过

---

### 2026-07-28 — 原型验证阶段（MVP-0）

**目标**：跑通"发现→分配→录入→归档"最小循环

#### 新增文件

| 文件 | 说明 |
|------|------|
| `index.html` | 页面骨架：顶栏 + Canvas主厅 + 右侧面板 + 底部日志 |
| `css/main.css` | 暗调琥珀色美学、档案目录风格 |
| `js/main.js` | 入口初始化、数据加载 |
| `js/game.js` | 资源管理、归档流程、时间系统、日志弹窗 |
| `js/ui.js` | 资源面板、存储室状态、归档条目渲染 |
| `js/canvas.js` | 圣所主厅：穹顶 + 歌者之座 + 声波可视化 |
| `data/archives.json` | 3条测试归档条目 |
| `data/vaults.json` | 1间测试存储室 |

#### 已实现功能

- [x] Canvas绘制圣所主厅（静态背景 + 动态声波环）
- [x] 资源面板（能源/介质/环境三数值实时显示）
- [x] 1间存储室状态显示（容量条）
- [x] 3条归档条目列表（含成本与过期时间）
- [x] 录入按钮（消耗资源 → 归档 → 反馈）
- [x] 归档完成弹窗（含守护者反应）
- [x] 底部日志系统（系统/守护者/成功三种类型）
- [x] 时间推进与条目过期机制
- [x] 资源不足/已归档/已消失状态处理

#### 验证结果

- 全部文件HTTP 200可访问
- 4个JS文件Node语法检查通过
- 20项ad-hoc验证全部通过

---

### 2026-07-28 — MVP扩展阶段

**目标**：扩展至3间存储室、12条条目、5名守护者、5个突发事件

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/archives.json` | 重写 | 扩展至12条条目（语言4条/历史4条/灾难4条） |
| `data/vaults.json` | 重写 | 扩展至3间存储室（语言/历史/灾难） |
| `data/guardians.json` | 新增 | 5名守护者数据（含4种对话类型） |
| `data/events.json` | 新增 | 5个突发事件（含选择分支与效果） |
| `index.html` | 修改 | 添加主题切换按钮、守护者面板、事件面板、存储室标签栏 |
| `css/main.css` | 重写 | 添加暗色/常规双主题、自定义滚动条、新组件样式 |
| `js/main.js` | 重写 | 添加主题初始化、加载守护者/事件数据 |
| `js/game.js` | 重写 | 添加守护者系统、事件系统、存储室切换 |
| `js/ui.js` | 重写 | 添加存储室标签栏渲染、守护者面板 |
| `js/canvas.js` | 重写 | 添加多闸门绘制、主题适配 |

#### 已实现功能

- [x] 3间存储室（语言与语法/历史编年/灾难纪实）
- [x] 12条归档条目（每间4条，含守护者反应）
- [x] 5名守护者（缇卡/芬恩/米莎/洛恩/埃塞尔）
- [x] 守护者对话系统（idle/archive/lowEnergy/event四种类型）
- [x] 5个突发事件（光雨渗透/能源波动/勘探发现/守护者分歧/设备故障）
- [x] 暗色/常规主题切换（LocalStorage持久化）
- [x] 自定义滚动条（Webkit + Firefox双兼容）
- [x] 存储室标签栏切换
- [x] 守护者面板（头像+姓名+角色+对话）
- [x] 事件通知横幅（含选择按钮）
- [x] 端口更换至8099

#### 验证结果

- 21项ad-hoc验证全部通过
- 覆盖：文件结构、JSON有效性、JS语法、HTML引用、CSS完整性、逻辑一致性

### 2026-07-28 — MVP完善阶段（模块A+B）

**目标**：日志系统改造 + Canvas场景切换

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 修改 | 底部日志区重构为按钮入口 + 展开面板 |
| `css/main.css` | 重写 | 日志按钮样式、展开面板动画、未读标记脉冲动画 |
| `js/game.js` | 修改 | 添加 `initLogSystem()`、`updateUnreadBadge()`、`clearUnread()` 函数 |
| `js/main.js` | 修改 | 初始化时调用 `initLogSystem()` |
| `js/canvas.js` | 重写 | 添加场景粒子系统、漂浮符号、场景定制背景/穹顶/声波/纹理 |

#### 已实现功能

**模块A：日志系统改造**
- [x] 底部固定日志栏 → 按钮入口（显示图标+文字）
- [x] 点击按钮展开悬浮式日志面板（max-height: 300px）
- [x] 面板顶部标题栏 + 关闭按钮
- [x] 未读消息红点标记（脉冲动画，99+上限）
- [x] 面板关闭时自动累计未读计数
- [x] 打开面板时清除未读 + 自动滚动到底部
- [x] 滑入/滑出动画（slideUp）
- [x] 双主题适配

**模块B：Canvas场景切换**
- [x] 三间存储室对应三种场景主题
- [x] 语言与语法：同心圆波纹 + 经纬线网格 + 蓝白上升粒子
- [x] 历史编年：时间轴圆环（虚线）+ 螺旋纹样 + 棕色尘埃漂浮
- [x] 灾难纪实：放射状裂纹 + 金色光雨粒子下落 + 暗红环境光
- [x] 穹顶颜色随场景变化（蓝/棕/红）
- [x] 声波颜色随场景变化
- [x] 漂浮符号系统（不同场景使用不同字符库）
- [x] 场景切换时自动清空并重建粒子/符号
- [x] 双主题适配（暗色/常规模式）

#### 验证结果

- 全部文件HTTP 200可访问
- 4个JS文件Node语法检查通过
- 4个JSON文件解析有效

### 2026-07-28 — MVP完善阶段（模块C+D）

**目标**：守护者互动增强 + 新手引导

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 修改 | 添加守护者互动菜单、新手引导遮罩层 |
| `css/main.css` | 修改 | 守护者菜单样式、推荐高亮动画、新手引导遮罩样式 |
| `js/game.js` | 修改 | 添加守护者互动、推荐归档、主动事件、新手引导系统 |
| `js/main.js` | 修改 | 初始化守护者互动 + 新手引导监听 |

#### 已实现功能

**模块C：守护者互动增强**
- [x] 点击守护者头像展开互动菜单（交谈 / 推荐归档）
- [x] 交谈：触发当前守护者的 idle 对话
- [x] 推荐归档：根据守护者技能匹配存储室，推荐成本最低的未归档条目
- [x] 推荐高亮：脉冲动画 + 自动滚动到目标 + 3秒后移除
- [x] 守护者主动事件（每6周~35%概率触发）
  - 缇卡的请求 — 补充介质 + 高亮祷文条目
  - 芬恩的发现 — 补充能源 + 高亮公约条目
  - 米莎的勘探报告 — 补充介质 + 高亮森林诗条目
  - 洛恩的的维护成果 — 补充大量能源
  - 埃塞尔的祈祷 — 恢复环境稳定
- [x] 接受/婉拒选择
- [x] 点击菜单外区域自动关闭

**模块D：新手引导**
- [x] 首次游玩自动触发（LocalStorage 持久化标记）
- [x] 6步渐进引导：主厅 → 存储室标签 → 守护者 → 资源 → 条目列表 → 结语
- [x] 半透明遮罩 + 高亮边框（amber色）
- [x] 浮动提示卡片（跟随目标位置）
- [x] 步进式按钮（"下一步"/"开始守护"）
- [x] 点击遮罩可跳过引导
- [x] 引导完成后不再显示

#### 验证结果

- 全部文件HTTP 200可访问
- 4个JS文件Node语法检查通过
- 4个JSON文件解析有效
- 59项ad-hoc验证全部通过
- 覆盖：HTML元素完整性、CSS动画关键帧、JS函数定义、JSON有效性

### 2026-07-28 — 中期第一阶段（存储室扩展）

**目标**：扩展至12间存储室 + 63条归档条目 + Canvas场景适配

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/vaults.json` | 重写 | 扩展至12间存储室（新增9间） |
| `data/archives.json` | 重写 | 扩展至63条归档条目（每间5-7条） |
| `css/main.css` | 修改 | 标签栏改为4列网格布局（原6列） |
| `js/canvas.js` | 修改 | 新增9种Canvas场景主题与特效 |

#### 已实现功能

**存储室扩展（12间）**
- [x] 语言与语法 / 历史编年 / 灾难纪实（原有3间）
- [x] 艺术与音乐 / 哲学与宗教 / 科学与技术（新增）
- [x] 生态与自然 / 法律与治理 / 日常生活（新增）
- [x] 建筑与工程 / 医学与生命 / 星象与天文（新增）

**归档条目（63条）**
- [x] 每间存储室5-7条，总计63条
- [x] 每条含守护者反应（主守护者+概率副守护者）
- [x] 守护者技能与存储室匹配机制

**Canvas场景（12种）**
- [x] 12间存储室对应12种场景主题
- [x] 每种场景有独特的：穹顶颜色 / 声波颜色 / 粒子颜色 / 特效符号
- [x] 新增场景特效：音符漩涡 / 莫比乌斯环 / 原子轨道 / 叶脉 / 天平 / 生活碎片 / 建筑网格 / 心跳波形 / 星轨

#### 验证结果

- 全部文件HTTP 200可访问
- 4个JS文件Node语法检查通过
- 4个JSON文件解析有效
- 26项ad-hoc验证全部通过

### 2026-07-28 — 第二阶段：存档系统 + 多周目继承

**目标**：实现 LocalStorage 存档系统与多周目继承机制

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 扩展 | 添加存档系统、多周目继承、封印圣所逻辑 |
| `index.html` | 修改 | 添加存档界面 Overlay、存档/读档按钮、标题画面 |
| `css/main.css` | 扩展 | 添加存档界面 + 标题画面样式（双主题适配） |
| `js/main.js` | 重写 | 添加标题画面系统、返回标题、主题默认亮色 |

#### 已实现功能

**存档系统**
- [x] 3 个存档槽位（可自由选择/覆盖/删除）
- [x] 完整状态序列化（资源/时间/归档/存储室使用量/事件记录）
- [x] 存档槽信息展示（周数/章节/归档数/时间戳/周目数）
- [x] 当前槽位追踪（自动保存至最近使用的槽位）
- [x] 存档界面（半透明遮罩 + 面板 + 动画）
- [x] 槽位选择面板（点击存档/读档后弹出）
- [x] 空槽位提示 + 新游戏入口
- [x] 删除确认 + 数据损坏保护

**多周目继承**
- [x] 周目计数追踪
- [x] 累积归档数统计
- [x] 最佳记录追踪（最多归档数 + 对应周数）
- [x] 周目奖励系统（2周目起获得起始资源加成）
- [x] 奖励随周目递增（能源/介质/环境稳定逐步提升）

**封印圣所（游戏完成）**
- [x] 10 周后可封印圣所
- [x] 封印统计面板（运行周数/归档数/利用率）
- [x] 多周目进度展示
- [x] 封印后触发 NG+ 计数
- [x] 新游戏自动应用 NG+ 奖励

**标题画面系统**
- [x] 新建游戏入口（选择存档槽位）
- [x] 读取存档入口
- [x] 帮助与关于入口
- [x] NG+ 进度展示（累计周目数、累计归档数）
- [x] 游戏中返回标题（自动保存当前进度）

**界面优化**
- [x] 顶栏新增「第 X 周」计数器（琥珀色高亮）
- [x] 存储室标签栏 6列→4列布局（完整显示名称）
- [x] 默认主题切换为亮色模式

#### 修复记录

| 问题 | 根因 | 修复 |
|------|------|------|
| 无法返回标题 | `getCurrentSlot()` 返回 0 时保存失败 | `if (currentSlot >= 1)` 守卫 |
| 存档/读档进不去 | 标题 `z-index: 11000` 覆盖存档界面 | 降至 `z-index: 1000` |
| 功能区不可用 | `initTitleScreen` 在容器隐藏后运行导致初始化失败 | 调整顺序：先初始化游戏系统 → 再显示标题 |
| 暗色看不清 | 默认主题 `dark` | 改为 `light` |
| 新建游戏点不了 | `renderSealButton` 在 `state=null` 时崩溃 | 添加空状态守卫 |
| 存储室名称截断 | 6列布局空间不足 | 改为4列布局 |

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 4 个 JSON 文件解析有效
- 14 项 ad-hoc 验证全部通过
- 覆盖：存档读档/标题切换/NG+计数/空状态守卫/默认主题

### 2026-07-29 — 第三阶段：生存压力系统

**目标**：实现资源自然衰减、圣所衰竭机制、跳过回合与过期时间调整

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/archives.json` | 修改 | 63条全部赋予过期时间（8周×12条、6周×41条、5周×5条、4周×5条） |
| `data/events.json` | 扩展 | 新增3个资源获取事件（勘探补给/守护者珍藏/地热涌动），总计8个 |
| `js/game.js` | 扩展 | 新增衰减系统、衰竭检测、跳过回合 |
| `js/main.js` | 修改 | 初始化衰竭状态 + 跳过回合监听 |
| `js/ui.js` | 修改 | 资源面板衰竭视觉指示 |
| `css/main.css` | 扩展 | 跳过按钮样式 + 衰竭脉冲动画 |
| `index.html` | 修改 | 底部功能栏新增「跳过」按钮 |

#### 已实现功能

**资源自然衰减**
- [x] 每回合自动消耗能源-2、介质-1、环境-1
- [x] 衰减通过 `getWeeklyDecay()` 配置，便于平衡调整

**圣所衰竭机制**
- [x] 能源归零：归档能源消耗×2（`energyMultiplier`）
- [x] 介质归零：无法录入新条目
- [x] 环境归零：条目过期速度翻倍（`getEffectiveExpiryWeeks()`）
- [x] 状态切换时自动日志通知
- [x] 资源衰竭时资源面板红色脉冲动画

**条目过期时间调整**
- [x] 关键条目（每间存储室第1条）：8周
- [x] 标准条目：6周
- [x] 历史编年(vault=2)：5周
- [x] 灾难纪实(vault=3)：4周

**跳过回合**
- [x] 底部功能栏新增「跳过」按钮（琥珀色高亮）
- [x] 跳过恢复：能源+8、介质+4、环境+3
- [x] 有事件时无法跳过
- [x] 守护者随机反应

**资源获取事件扩充**
- [x] 地表勘探补给（3选1：能源/介质/全收）
- [x] 守护者的珍藏（埃塞尔赠介质）
- [x] 地热涌动（能源vs环境权衡）

**存档兼容**
- [x] 衰竭状态序列化/反序列化
- [x] 新游戏自动初始化 `deterioration: {energy:false, media:false, environment:false}`

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 4 个 JSON 文件解析有效
- 30项 ad-hoc 验证全部通过
- 覆盖：衰减计算、衰竭检测、过期调整、跳过回合、存档序列化、CSS样式

---

## 当前项目结构

```
memory-sanctuary/
├── index.html                  # 入口页面
├── css/
│   └── main.css                # 全局样式（双主题+滚动条）
├── js/
│   ├── main.js                 # 入口初始化+标题画面+主题
│   ├── game.js                 # 核心逻辑（资源/归档/时间/守护者/事件/存档）
│   ├── ui.js                   # UI渲染（资源/标签/条目/周数）
│   └── canvas.js               # 圣所主厅Canvas绘制
├── data/
│   ├── archives.json           # 63条归档条目
│   ├── vaults.json             # 12间存储室
│   ├── guardians.json          # 5名守护者
│   └── events.json             # 5个突发事件
└── README.md                   # 项目说明
```

---

## 代码统计

| 类型 | 文件数 | 约总行数 |
|------|--------|----------|
| HTML | 1 | ~200 |
| CSS | 1 | ~1520 |
| JavaScript | 4 | ~2950 |
| JSON数据 | 4 | ~650 |
| **合计** | **10** | **~5320** |

---

## 已知问题（待修复）

| 问题 | 优先级 | 说明 |
|------|--------|------|
| 选择无差异化 | 中 | 归档哪条对结局无影响 |
| 缺乏失败条件 | 中 | 无真正的"游戏结束"状态 |
| 守护者主动事件概率低 | 低 | 每6周~35%，实际很少触发 |

### 2026-07-29 — 第五阶段：UX优化

**目标**：解决玩家反馈的5个体验问题

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 修改 | 移除过期警告日志，新增困局检测+横幅提醒 |
| `js/ui.js` | 修改 | 新增即将过期聚合面板，过期条目视觉增强 |
| `css/main.css` | 修改 | 困局横幅样式，存储室标签完整显示，过期条目删除线 |
| `js/main.js` | 修改 | 初始化困局横幅 |

#### 已实现功能

**1. 过期警告移至聚合面板**
- [x] 移除 `onTimeAdvanced` 中的 `remaining <= 4` 警告日志
- [x] 条目列表顶部新增红色脉冲「⚠ 即将过期」聚合面板
- [x] 即将过期条目标题旁显示「⚠ 即将消失」标记

**2. 存储室名称完整显示**
- [x] 标签改为 `white-space: normal` + `word-break: break-all`
- [x] 固定高度 + flex居中，确保2行文字完整可见

**3. 跳过受阻时横幅提醒**
- [x] `showSkipBlockedBanner()` 替代日志提示
- [x] 3秒后自动恢复困局状态

**4. 过期条目视觉增强**
- [x] 新增 `.entry-item.expired` 类（红色半透明背景）
- [x] 标题红色删除线（`text-decoration-thickness: 2px`）
- [x] 整体透明度降至40%

**5. 困局提示与引导机制**
- [x] `checkStuckState()` 检测：无可用操作 + 有潜在条目
- [x] 顶部横幅显示原因（能源/介质耗尽）
- [x] 提供「跳过回合」和「封印圣所」快捷链接
- [x] 所有条目完成时显示绿色成功横幅

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 25项 ad-hoc 验证全部通过

### 2026-07-29 — 第六阶段：游戏完整性

**目标**：添加失败条件、周数上限、事件反馈、归档确认

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 扩展 | 失败检测、周数上限、游戏结束、事件反馈、归档确认 |
| `js/main.js` | 修改 | 初始化 gameOver/guardianMoods/scheduledEvents |
| `js/ui.js` | 修改 | 周数进度条、归档确认调用 |
| `css/main.css` | 扩展 | 进度条样式、确认弹窗样式 |
| `index.html` | 修改 | 周数进度条容器 |
| `data/events.json` | 重写 | 所有8个事件添加 feedback 字段 |

#### 已实现功能

**失败条件**
- [x] 三种资源全部归零 → 圣所崩溃结局
- [x] 区别于主动封印的失败叙事

**周数上限**
- [x] MAX_WEEK = 20周
- [x] 超时自动触发终局
- [x] 顶栏周数进度条（颜色随紧迫度变化）

**事件结果反馈**
- [x] `applyEventFeedback()` 系统
- [x] 叙事标记（narrativeFlags）
- [x] 守护者好感度（guardianMoods）
- [x] 未来事件调度（scheduledEvents）
- [x] 所有8个事件添加 feedback 字段

**归档确认**
- [x] 点击「录入归档」弹出确认弹窗
- [x] 显示消耗、存储室、关联条目
- [x] 「不再提示」复选框（LocalStorage持久化）
- [x] 取消按钮可关闭弹窗

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 30项 ad-hoc 验证全部通过

---

## 下一步计划

### 短期（完整版 — 路线A：生存压力） ✅ 已完成

- [x] **资源自然衰减系统** — 每回合自动消耗能源-2、介质-1、环境-1
- [x] **圣所衰竭机制** — 三种归零状态，各有独特惩罚
- [x] **条目过期时间调整** — 63条全部赋予4-8周过期时间
- [x] **跳过回合决策** — 跳过恢复资源，有事件时不可用
- [x] **资源获取事件扩充** — 新增3个事件（勘探/珍藏/地热）

### 2026-07-29 — 第五阶段：UX优化

**目标**：解决玩家反馈的5个体验问题

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 修改 | 移除过期警告日志，新增困局检测+横幅提醒 |
| `js/ui.js` | 修改 | 新增即将过期聚合面板，过期条目视觉增强 |
| `css/main.css` | 修改 | 困局横幅样式，存储室标签完整显示，过期条目删除线 |
| `js/main.js` | 修改 | 初始化困局横幅 |

#### 已实现功能

**1. 过期警告移至聚合面板**
- [x] 移除 `onTimeAdvanced` 中的 `remaining <= 4` 警告日志
- [x] 条目列表顶部新增红色脉冲「⚠ 即将过期」聚合面板
- [x] 即将过期条目标题旁显示「⚠ 即将消失」标记

**2. 存储室名称完整显示**
- [x] 标签改为 `white-space: normal` + `word-break: break-all`
- [x] 固定高度 + flex居中，确保2行文字完整可见

**3. 跳过受阻时横幅提醒**
- [x] `showSkipBlockedBanner()` 替代日志提示
- [x] 3秒后自动恢复困局状态

**4. 过期条目视觉增强**
- [x] 新增 `.entry-item.expired` 类（红色半透明背景）
- [x] 标题红色删除线（`text-decoration-thickness: 2px`）
- [x] 整体透明度降至40%

**5. 困局提示与引导机制**
- [x] `checkStuckState()` 检测：无可用操作 + 有潜在条目
- [x] 顶部横幅显示原因（能源/介质耗尽）
- [x] 提供「跳过回合」和「封印圣所」快捷链接
- [x] 所有条目完成时显示绿色成功横幅

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 25项 ad-hoc 验证全部通过

---

### 2026-07-29 — 第六阶段：游戏完整性

**目标**：添加失败条件、周数上限、事件反馈、归档确认

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 扩展 | 失败检测、周数上限、游戏结束、事件反馈、归档确认 |
| `js/main.js` | 修改 | 初始化 gameOver/guardianMoods/scheduledEvents |
| `js/ui.js` | 修改 | 周数进度条、归档确认调用 |
| `css/main.css` | 扩展 | 进度条样式、确认弹窗样式 |
| `index.html` | 修改 | 周数进度条容器 |
| `data/events.json` | 重写 | 所有8个事件添加 feedback 字段 |

#### 已实现功能

**失败条件**
- [x] 三种资源全部归零 → 圣所崩溃结局
- [x] 区别于主动封印的失败叙事

**周数上限**
- [x] MAX_WEEK = 20周
- [x] 超时自动触发终局
- [x] 顶栏周数进度条（颜色随紧迫度变化）

**事件结果反馈**
- [x] `applyEventFeedback()` 系统
- [x] 叙事标记（narrativeFlags）
- [x] 守护者好感度（guardianMoods）
- [x] 未来事件调度（scheduledEvents）
- [x] 所有8个事件添加 feedback 字段

**归档确认**
- [x] 点击「录入归档」弹出确认弹窗
- [x] 显示消耗、存储室、关联条目
- [x] 「不再提示」复选框（LocalStorage持久化）
- [x] 取消按钮可关闭弹窗

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 30项 ad-hoc 验证全部通过

---

### 2026-07-29 — Bug修复

**目标**：修复玩家反馈的4个问题

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 修改 | 确认弹窗关闭按钮、困局横幅自动创建、时间上限返回标题、跳过限制、衰减加速 |

#### 已实现功能

**1. 归档确认关闭按钮行为隔离**
- [x] 新增 `closeConfirmModal()` 统一处理
- [x] 确认弹窗关闭后重置为「确定」
- [x] 正常归档弹窗不受影响

**2. 困局横幅渲染修复**
- [x] `checkStuckState()` 自动创建缺失的 banner
- [x] 无需等待 `initStuckBanner()` 先执行

**3. 时间上限后返回标题**
- [x] 崩溃结局按钮改为「返回标题」
- [x] 调用 `showTitleScreen()` 而非 `location.reload()`

**4. 跳过回合限制**
- [x] `skipTurn()` 增加 `gameOver` 检查
- [x] `skipTurn()` 增加 `week >= MAX_WEEK` 检查
- [x] 达到上限后调用 `checkWeekLimit()` 触发终局

**5. 衰减加速机制**
- [x] `getWeeklyDecay()` 根据归零资源数量调整衰减速度
- [x] 2种资源归零后，剩余资源衰减×2
- [x] 基础衰减值提升（3/2/1.5）

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 25项 ad-hoc 验证全部通过

---

### 中期（完整版 — 路线B：叙事关联） ✅ 已完成

- [x] **条目线索链** — 61/63条含关联，归档自动揭示🔗，条目列表显示链式进度
- [x] **文明图谱** — Canvas环形12节点，显示完成度+链式连线，底部「图谱」按钮打开
- [x] **封印总结系统** — 文明画像算法（7种称号），基于存储室占比+组合识别
- [x] **隐藏结局** — 7种结局（永恒记忆/星空之歌/灾难见证者/生命回响/追光者/爱与记忆/寂静圣所）

### 2026-07-29 — 第四阶段：叙事关联系统

**目标**：实现条目线索链、文明图谱、封印总结与隐藏结局

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/archives.json` | 修改 | 61条添加 relatedArchives（5条大链，每条5-6条） |
| `js/game.js` | 扩展 | 新增线索链检测、文明图谱绘制、画像生成、隐藏结局判定 |
| `js/main.js` | 修改 | 初始化文明图谱 |
| `js/ui.js` | 修改 | 条目列表显示🔗链式标记 |
| `css/main.css` | 扩展 | 图谱按钮+图谱界面样式 |
| `index.html` | 修改 | 底部功能栏新增「图谱」按钮 + atlas-overlay |

#### 已实现功能

**条目线索链**
- [x] 61/63条含 relatedArchives（5条大链）
- [x] 归档后自动检测并日志通知「🔗 线索揭示」
- [x] 条目列表显示链式进度（🔗 → 🔗2/3 → ✅）

**文明图谱**
- [x] Canvas绘制12存储室节点（环形布局）
- [x] 已归档存储室填充主题色
- [x] 进度弧线显示完成比例
- [x] 虚线连接已完成的跨存储室链
- [x] 整体文明完整度百分比

**封印总结系统（文明画像）**
- [x] 画像生成算法：存储室占比+组合识别
- [x] 7种称号：永恒记忆/文明守护者/歌与诗之声/律法与秩序/追光者/生命回响/灾难见证者/星空之梦/爱与记忆/碎片收集者/微光守护者/寂静圣所

**隐藏结局**
- [x] 7种结局条件判定（完全记忆/星空之歌/灾难链/生态链/科学链/日常生活链/0归档）
- [x] 封印时优先显示隐藏结局弹窗
- [x] 封印弹窗显示文明画像

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 65项 ad-hoc 验证全部通过
- 覆盖：线索链检测、图谱绘制、画像算法、结局判定、UI指示、CSS样式

### 2026-07-29 — 守护者好感度 + 调度事件系统

**目标**：实装守护者好感度影响与 scheduledEvents 调度，增强游戏中期体验

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/guardians.json` | 扩展 | 5名守护者各新增5级 moodDialogues（敌对/冷淡/中立/友好/亲密） |
| `data/events.json` | 扩展 | 8个事件添加 futureEvent 字段 + 新增10个专属调度事件 |
| `js/game.js` | 扩展 | 好感度5函数 + 调度系统 + 持续效果 + 推荐策略变化 + 封印奖励 |
| `js/ui.js` | 扩展 | `renderGuardianMood()` 好感度UI渲染 |
| `js/main.js` | 修改 | 加载 scheduledEvents + 初始化 unlockedBonuses |
| `css/main.css` | 扩展 | 好感度表情样式 + 守护者面板边框颜色 |
| `index.html` | 修改 | 守护者面板新增好感度指示器 |

#### 已实现功能

**好感度系统**
- [x] 5级好感度分层：敌对(≤-3)/冷淡(<0)/中立(0-2)/友好(3-4)/亲密(≥5)
- [x] 对话变化：idle 类型对话根据好感度层级动态选取
- [x] 推荐策略：好感度≥3时优先推荐高价值条目而非低成本
- [x] 主动事件频率：好感度≥2的守护者存在时，概率从35%提升至50%
- [x] 封印奖励：3名以上守护者好感度≥3时，NG+额外+10起始能源
- [x] UI指示：守护者面板显示💔💙🤍💛❤️ + 面板边框颜色变化
- [x] 存档总结：封印弹窗显示守护者关系总结

**调度事件系统**
- [x] `processScheduledEvents()`：每周检查并触发到期事件
- [x] `applySustainedBonuses()`：持续效果（如每回合+2/+3能源）
- [x] 8个原有事件添加 futureEvent 触发器
- [x] 10个专属调度事件：守护者个人事件/叙事连锁/晚期危机
- [x] 解锁奖励类型：energy_per_turn_2 / energy_per_turn_3
- [x] 调度事件数据序列化/反序列化

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 2 个 JSON 文件解析有效
- 代码总量：~5,394 行（+74行）

### 2026-07-29 — Bug修复：标题画面按钮无响应

**问题**：标题画面四个按钮（新建/读取/帮助/关于）全部卡死，无法点击。

**根因**：`js/main.js` 的 `loadGameData()` 函数中对 `eventsRes` Response 对象调用了两次 `.json()`：

```js
MemorySanctuary.data.events = (await eventsRes.json()).events;
MemorySanctuary.data.scheduledEvents = (await eventsRes.json()).scheduledEvents || [];
```

第二次调用时抛出 `TypeError: Body has already been consumed`，导致 `loadGameData()` 异常中断。由于 `initTitleScreen()` 在 `loadGameData()` 之后执行，异常使得标题按钮的事件监听器从未被绑定。

**修复**：将两次 `.json()` 调用合并为一次，缓存结果后分别取属性：

```js
const eventsData = await eventsRes.json();
MemorySanctuary.data.events = eventsData.events;
MemorySanctuary.data.scheduledEvents = eventsData.scheduledEvents || [];
```

**修改文件**：`js/main.js`（第 58-60 行）

**验证结果**：
- 8项 ad-hoc 验证全部通过
- 双重 `.json()` 调用会抛出异常（证明原Bug存在）
- 单次 `.json()` + 属性访问正常工作
- 标题画面按钮恢复正常

### 长期（打磨）

- [x] **守护者好感度影响对话/推荐/结局** — 好感度系统已实装
- [x] **scheduledEvents实际调度新事件** — 调度系统已实装
- [x] **紧急归档条目** — 5条第8周后出现的紧急归档已实装
- [x] **守护者终章对话** — 第15周后好感度达标触发特殊对话
- [x] **圣所维护项目** — 3个项目（发电机/冷却系统/卫星天线）已实装
- [x] **终局倒计时事件** — 第18-20周强制触发特殊叙事
- [ ] 地表勘探完整系统（基础框架已搭，待扩展反馈机制）
- [ ] 音频系统（Web Audio API）
- [ ] 性能优化
- [ ] 部署至 GitHub Pages

---

### 2026-07-29 — 子系统D：后期内容扩充

**目标**：实装后期内容扩充，解决中后期体验空洞化问题，增强终局紧迫感与叙事深度。

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/archives.json` | 扩展 | 新增5条「紧急归档」，含 `availableAfter: 8`、`emergency: true`、`expiresAfter: 12` |
| `data/guardians.json` | 扩展 | 5名守护者各添加 `finaleDialogues`（friendly/intimate 两种终章对话） |
| `data/events.json` | 扩展 | 新增3个终局倒计时事件（第18/19/20周强制触发） |
| `data/projects.json` | 新建 | 3个圣所维护项目（修复发电机/冷却系统/卫星天线） |
| `js/main.js` | 修改 | 加载 `projects.json`，初始化 `activeProjects`/`completedProjects`，调用 `initProjects()` |
| `js/game.js` | 修改 | 项目系统（init/canStart/start/process/applyEffect）、终章对话触发、终局事件强制触发 |
| `js/ui.js` | 修改 | `updateProjectButton()`、`renderProjectList()`、紧急归档标识渲染 |
| `css/main.css` | 修改 | 项目面板样式（overlay/panel/item/button）、紧急归档红边标识 |
| `index.html` | 修改 | 底部栏添加 `project-btn`，新建 `project-overlay` 面板容器 |

#### 已实现功能

**紧急归档**
- [x] 第8周后出现5条紧急归档条目
- [x] 过期时间12周（游戏结束前必定可归档）
- [x] UI上显示红色边框+⚠标识
- [x] `availableAfter` 字段控制显示时机，未到周数不渲染
- [x] 条目内容：最后广播、地热报告、辐射读数、加密记录×2

**守护者终章对话**
- [x] 第15周后，好感度 tier 为 friendly/intimate 时触发特殊对话
- [x] 每个守护者独立2条（friendly 1条 + intimate 1条）
- [x] 不影响低好感度守护者的正常对话

**圣所维护项目**
- [x] 第10周后可开始项目
- [x] 3个项目：修复备用发电机（+2能源/周，3周）、修复冷却系统（介质衰减-50%，4周）、重启卫星天线（解锁2条加密记录，2周）
- [x] 消耗资源（能源+介质），有持续时间和守护者好感度加成
- [x] 项目面板UI：进行中/可开始/已完成/资源不足/未解锁状态区分
- [x] 冷却系统可重复，其他一次性
- [x] 洛恩/埃塞尔/芬恩好感度达标时触发额外加成

**终局倒计时事件**
- [x] 第18周：地热警告（管道结晶化）
- [x] 第19周：穹顶裂痕（圣所结构损坏）
- [x] 第20周：最后的光（能源归零，封印圣所）
- [x] 强制触发（不占用随机事件位），增强紧迫感

**存档兼容性**
- [x] `activeProjects`/`completedProjects` 纳入序列化/反序列化
- [x] 旧存档读档时自动初始化为空数组（`|| []`）

#### 验证结果

- 4 个 JS 文件 Node 语法检查通过
- 6 个 JSON 文件结构合法
- 64 项 ad-hoc 验证全部通过
- 覆盖：数据结构、函数签名、UI渲染、存档序列化、CSS/HTML完整性

---

### 2026-07-29 — Bug修复：标题画面按钮无响应（initProjects 空状态）

**问题**：子系统D更新后，游戏标题画面四个按钮全部点不动。已清空缓存刷新、使用无痕窗口，依然无效。

**根因**：`initProjects()` 在初始化链中被调用，但此时 `MemorySanctuary.state` 仍是 `null`。访问 `null.activeProjects` 抛出 TypeError，被 try-catch 吞掉 → `initTitleScreen()` 不执行 → 标题按钮无事件监听器。

**修复**：在 `initProjects()` 开头添加 `if (!MemorySanctuary.state) return;`，使其安全退出，不阻塞后续初始化。

**修改文件**：`js/game.js`（`initProjects()` 函数）

**验证结果**：
- 12 项 ad-hoc 验证全部通过（initProjects 空状态防护 + 初始化链顺序 + 语法 + 数据完整性）

---

### 2026-07-29 — Bug修复：读档后游戏空壳化

对 `null` 设置属性抛出 `TypeError`，被 try-catch 中的 `catch` 块吞掉并返回 `false`。但此时标题画面已隐藏（`title-screen.classList.add('hidden')`）、游戏容器已显示（`gameContainer.classList.remove('hidden')`）—— 玩家看到的是一个空壳化的游戏界面，所有依赖 `state` 的渲染和交互全部失效。

而新建游戏路径中 `startNewGame()` 调用了 `initGameState()`，所以新建游戏一切正常。这验证了"旧版本存档不兼容"的猜想——实际上是读档逻辑本身的缺陷。

**修复**：在 `loadGame()` 的 try 块开头、`JSON.parse(raw)` 之后，添加 `initGameState()` 调用，先初始化完整的默认状态，再用存档数据逐字段覆盖。

```js
try {
    const saveData = JSON.parse(raw);

    // Initialize fresh state before loading
    initGameState();

    MemorySanctuary.state.resources = { ...saveData.state.resources };
    // ... 后续字段覆盖 ...
}
```

**修改文件**：`js/game.js`（第 1848-1850 行）

**验证结果**：
- 4 个 JS 文件 Node 语法检查通过
- 34 项 ad-hoc 验证全部通过（覆盖：initGameState 调用顺序、状态结构完整性、skipTurn 状态访问、atlas 状态访问、renderAll 渲染函数调用链、存档序列化/反序列化）

---

### 2026-07-29 — 子系统C：地表勘探系统

**目标**：实现可派遣守护者的地表勘探系统，解决后期无事可做的问题，融入生存+肉鸽元素。

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/explorations.json` | 新增 | 12个勘探点，含难度/耗时/技能要求/4种结果 |
| `css/main.css` | 扩展 | 底部按钮视觉区分 + 勘探界面全套样式 |
| `index.html` | 修改 | 底部功能栏新增「勘探」按钮 + explore-overlay |
| `js/main.js` | 修改 | 加载探索数据 + 初始化 exploration 状态 |
| `js/game.js` | 扩展 | 新增勘探系统全套逻辑（~280行） |
| `js/ui.js` | 扩展 | 新增 renderExplorationButton() |

#### 已实现功能

**底部功能栏视觉优化**
- [x] 跳过按钮：警告色（橙色）高亮
- [x] 图谱按钮：琥珀色高亮
- [x] 勘探按钮：新增绿色高亮（#5aa86e）
- [x] 存档/读档：半透明琥珀色（降低存在感）
- [x] 标题/帮助/关于：灰色弱化

**地表勘探数据（12个勘探点）**
- [x] 寂静之城·中央广场（难度1，无技能要求）
- [x] 枯萎之森（难度2，需生态+生存）
- [x] 星象台遗迹（难度2，需工程）
- [x] 生命维持中心（难度1，需生存）
- [x] 中央图书馆（难度1，需档案）
- [x] 工业制造区（难度3，需工程+维护）
- [x] 信仰之殿（难度1，需宗教+哲学）
- [x] 边缘住宅区（难度1，无技能要求）
- [x] 地热反应堆（难度3，需工程+能源）
- [x] 封存者地窖（难度2，需勘探）
- [x] 轨道发射场（难度3，需工程+能源）
- [x] 终末纪念碑（难度1，无技能要求）

**勘探机制**
- [x] 每个勘探点有4种结果：资源获取/叙事/风险
- [x] 派遣守护者影响结果概率（匹配技能降低风险、提高收益）
- [x] 勘探耗时1-2周，期间按钮禁用
- [x] 返回时自动弹出结果面板
- [x] 派遣守护者好感度+1
- [x] 结果概率实时显示（进度条可视化）

**存档兼容**
- [x] exploration 状态序列化/反序列化
- [x] 新游戏自动初始化 exploration 状态

#### 验证结果

- 全部文件 HTTP 200 可访问
- 4 个 JS 文件 Node 语法检查通过
- 5 个 JSON 文件解析有效
- 73 项 ad-hoc 验证全部通过
- 覆盖：文件结构、数据完整性、技能匹配、CSS样式、HTML元素、JS函数定义、存档序列化

---

## 参考游戏

| 游戏 | 借鉴要素 |
|------|----------|
| What Remains of Edith Finch | 碎片化叙事 |
| Disco Elysium | 文字驱动、内心叙事 |
| Frostpunk | 末日资源管理、道德抉择 |
| This War of Mine | 生存压力、人员管理 |
| The Return of the Obra Dinn | 考古推理、碎片拼凑 |
| Heaven's Vault | 考古语言学、叙事驱动 |

---

### 2026-07-30 — 第七章：衰败与回响（P0 视听沉浸系统）

**目标**：通过视听反馈让圣所的衰败"可感"，解决"可游玩内容少、缺乏试听沉浸感"的核心问题。

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/audio.js` | 新建 | Web Audio API 环境音系统（304行） |
| `js/canvas.js` | 扩展 | 衰败视觉层 `drawDecayOverlay()`（+90行） |
| `js/game.js` | 扩展 | 归档/衰竭/过期节点音效触发（+15行） |
| `js/main.js` | 扩展 | 音频延迟初始化 + 静音切换按钮 |
| `css/main.css` | 扩展 | 衰败脉冲动画 + 静音按钮样式 |
| `index.html` | 修改 | 添加 `mute-toggle` 按钮 + `audio.js` 引用 |

#### 已实现功能

**衰败视觉层（Canvas）**
- [x] 能源归零：红色呼吸脉冲（穹顶闪烁 + 全屏偏红）
- [x] 能源<30%：间歇性红色闪烁（sin波动阈值触发）
- [x] 介质归零：30个雪花噪点粒子随机漂移
- [x] 介质<20%：10个轻度噪点
- [x] 环境归零：暗角加重（径向渐变，边缘60%红黑）
- [x] 环境<30%：轻度暗角
- [x] 三资源同时危急：叠加震颤效果（sin振幅调制）
- [x] 终局倒计时（第16周起）：边缘逐渐泛红，随week递增

**环境音系统（audio.js）**
- [x] 基础drone层：60Hz正弦波 + LFO调制，音量随能源降低而升高
- [x] 归档成功音效：4频率风铃（800/1000/1200/1500Hz，错峰触发）
- [x] 资源衰竭警示：三角波下行滑音（200→100Hz）
- [x] 条目过期碎裂：高通白噪声burst
- [x] 场景切换音效：12种场景各有特征频率/波形（如灾难=锯齿波、天文=正弦高频）
- [x] 终局心跳：第18周起启动，频率随week加速（1200ms→500ms间隔）
- [x] 静音切换：顶栏🔊/🔇按钮 + AudioSystem.toggleMute()
- [x] 延迟初始化：首次用户交互后触发AudioContext（符合浏览器策略）

**CSS衰败效果**
- [x] `.resource.deterioration` 类：红色边框 + 脉冲阴影动画
- [x] `@keyframes deteriorationPulse`：0→1循环（4px→16px红晕）

#### 验证结果

- 5个JS文件Node语法检查全部通过
- 全部文件HTTP 200可访问（含新增audio.js）
- 音频系统独立模块（不侵入原有架构）
- 衰败视觉层仅追加绘制，不改动原有Canvas逻辑
- 所有AudioSystem调用均有 `typeof AudioSystem !== 'undefined'` 守卫

### 2026-07-30 — Bug修复4：设置系统 + 多个关键运行时Bug

**目标**：修复设置面板、音频系统、归档流程、勘探系统的多个运行时错误

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `index.html` | 修改 | 标题菜单加「⚙️ 设置」按钮；底部功能栏加齿轮按钮；新增设置面板 overlay |
| `css/main.css` | 修改 | 设置面板全套样式；`.week-value.updated` 脉冲动画 |
| `js/main.js` | 修改 | 新增 `getSettings()` / `initSettings()` / `openSettingsPanel()` / `closeSettingsPanel()` |
| `js/game.js` | 修改 | `confirmArchive` 改用 settings 对象；`archiveEntry` 结果弹窗受 `showResult` 控制；新增 `adjustGuardianMood()` / `adjustResource()` |
| `js/ui.js` | 修改 | `renderWeekDisplay` 加周数变化脉冲动画 |
| `js/audio.js` | 修改 | 所有 `setAtTime` 改为 `setValueAtTime`（标准 API）；drone 初始增益 0.02；`init()` 末尾加 `ctx.resume()` |

#### 已修复Bug

**Bug 1：归档按钮无弹窗**
- 根因：localStorage 残留 `skip-confirm=true`（之前测试时勾选「不再提示」）
- 修复：新增设置面板，用户可随时开关「跳过归档确认」
- 附加修复：`<p id="modal-content">` 改为 `<div>`（`<p>` 不能包含块级元素）

**Bug 2：音频按钮无效**
- 根因：`AudioContext` 创建后处于 `suspended` 状态，未调用 `resume()`
- 修复：`init()` 末尾加 `ctx.resume()`；drone 初始增益从 0 改为 0.02

**Bug 3：归档/跳过后资源不更新、周数不动**
- 根因：`audio.js` 中使用了非标准 API `setAtTime`，浏览器不支持导致抛出异常
- 异常堆栈：`archiveEntry` → `advanceTime(1)` → `onTimeAdvanced` → `updateDroneByEnergy` → 崩溃
- 崩溃导致 `renderAll()` 永远不执行，所以 UI 不更新
- 修复：所有 `setAtTime` 改为标准 API `setValueAtTime`

**Bug 4：勘探系统 `adjustGuardianMood` / `adjustResource` 未定义**
- 根因：函数被调用但从未定义
- 修复：补全 `adjustGuardianMood(guardianId, delta)` 和 `adjustResource(resource, amount)` 函数

**Bug 5：周数变化无视觉反馈**
- 根因：数字更新但无过渡动画，玩家难以感知
- 修复：`renderWeekDisplay` 检测数字变化时添加 `.updated` 类，触发 0.6s 脉冲动画

#### 关键经验教训

1. **一个看似无关的 bug 可以拖垮整个功能链**
   - 音频系统错误 → 归档/跳过功能崩溃 → UI 不更新
   - 修复方法：看堆栈追踪的**起点**，不要只看表面症状

2. **Web Audio API 的标准方法名**
   - `AudioParam.setValueAtTime()` ✓ 标准
   - `AudioParam.setAtTime()` ✗ 非标准（某些浏览器支持，但不可靠）
   - 同样：`linearRampToValueAtTime` / `exponentialRampToValueAtTime` 是标准的

3. **localStorage 持久化设置必须提供 UI 入口**
   - 用户勾选「不再提示」后，设置持久化在 localStorage
   - 如果没有设置面板，用户无法取消该选项
   - 建议：所有持久化设置都应有对应的 UI 开关

4. **HTML 标签的嵌套规则**
   - `<p>` 不能包含块级元素（`<div>`, `<p>`, `<ul>` 等）
   - 浏览器会自动把块级元素挪到 `<p>` 外面，导致 DOM 结构异常
   - 修复：把 `<p id="modal-content">` 改为 `<div id="modal-content">`

5. **函数定义遗漏的排查**
   - `adjustGuardianMood` 和 `adjustResource` 被调用但从未定义
   - 这类错误只在特定路径触发（勘探结果），容易被遗漏
   - 建议：定期用 `grep -rn "functionName" src/` 检查未定义引用

6. **`addLog` 参数顺序**
   - 原代码：`addLog('system', message)` — 错误
   - 正确：`addLog(message, 'system')` — 函数签名是 `(message, type)`
   - 这种错误不会崩溃，但会导致日志类型显示错误

#### 验证结果

- 全部文件 HTTP 200 可访问
- 5 个 JS 文件 Node 语法检查通过
- 设置面板可正常打开/关闭，设置持久化到 localStorage
- 归档流程完整：确认弹窗 → 归档 → 结果弹窗 → UI 更新
- 跳过功能完整：日志记录 → 资源恢复 → 周数更新 → 脉冲动画
- 音频系统正常：drone 低音持续，归档风铃，静音切换
- 勘探系统无 `ReferenceError`

---

*本日志由开发助手与玩家协作维护，每次修改均追加时间戳记录。*

### 2026-07-30 — 方向一：压力与抉择深化

**目标**：让资源管理真正有紧张感，解决"失败条件形同虚设"和"中后期体验空洞"两个核心问题。

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 修改 | 失败条件放宽（2种归零）、跳过代价（30%好感-1）、应急协议系统 |
| `js/ui.js` | 修改 | 新增 `updateEmergencyButton()` 函数 |
| `js/audio.js` | 修改 | 新增 `playMechanicalEngage()` 机械启动音效 |
| `css/main.css` | 修改 | 项目面板opacity+scale动画、应急按钮红色脉冲样式 |
| `index.html` | 修改 | 底部功能栏新增「应急」按钮 |

#### 已实现功能

**1. 失败条件放宽**
- 原条件：三种资源全部归零
- 新条件：任意两种资源归零即触发崩溃
- 大幅提升生存压力，20周内可能触发坏结局

**2. 跳过回合代价**
- 30%概率触发守护者好感度-1
- 5条专属抱怨对话（不同于普通跳过对话）
- 触发时播放警示音效
- 玩家必须权衡"恢复资源"vs"守护者关系"

**3. 应急协议系统**
- 新增底部「应急」按钮（红色高亮）
- 按钮状态：正常禁用 / 资源危急时启用（红色脉冲）
- 效果：牺牲15点环境稳定度 → 获得25点能源
- 可用条件：环境稳定度 > 15，或任意资源归零/危急
- 激活时播放机械启动音效 + 守护者特殊反应叙事
- 不可激活时弹窗提示"圣所状况已至极境"

**4. 项目面板反馈增强**
- 打开时触发 opacity + scale 淡入动画（0.2s）
- 打开时播放 `playMechanicalEngage()` 机械音效
- 音效由锯齿波80Hz→120Hz→60Hz滑音 + 方波泛音组成

#### 数值平衡调整

| 数值 | 旧值 | 新值 | 理由 |
|------|------|------|------|
| 失败条件 | 3种归零 | 2种归零 | 让坏结局实际可触发 |
| 跳过恢复量 | 能源+8/介质+4/环境+3 | 不变 | 维持原有恢复效率 |
| 跳过代价 | 无 | 30%好感-1 | 防止无脑跳过 |
| 应急协议 | 不存在 | 环境-15→能源+25 | 提供绝境翻盘手段 |
| 应急触发条件 | - | 环境>15 | 防止无限使用 |

#### 验证结果

- 5个JS文件 Node 语法检查通过
- 14项 ad-hoc 验证全部通过
- 覆盖：失败条件逻辑、跳过代价概率、应急协议数值、按钮状态管理、CSS动画、音效定义、HTML元素完整性

---

### 2026-07-31 — 地表勘探反馈机制（四层增强）

**目标**：解决勘探系统"反馈弱"问题，通过四层渐进式增强让勘探行动有重量感、叙事联动和长期影响。

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/explorations.json` | 扩展 | 12个勘探点添加 `maxAttempts`、`guardianSpecials`、`revealsClue` |
| `data/guardians.json` | 扩展 | 5名守护者各添加 `explorationDialogues`（共7条专属对话） |
| `js/audio.js` | 扩展 | 新增4个音效函数（出发/资源/叙事/风险返回） |
| `js/game.js` | 扩展 | 新增6个函数 + 修改4个现有函数（~200行） |
| `js/main.js` | 修改 | `initGameState` 添加 `completedExplorations`/`fatigue`/`explorationLog` |
| `js/ui.js` | 不变 | （CSS已覆盖） |
| `css/main.css` | 扩展 | 新增~120行样式（结果分色/动画/横幅/日志/标签页） |
| `index.html` | 修改 | 添加 `explore-tabs`/`explore-log-container`/`result-icon`/`explore-return-banner` |

#### 已实现功能

**Layer 1 — 即时视听反馈**
- [x] 结果面板三色分色：资源=金色边框/叙事=青蓝边框/风险=红色边框
- [x] 结果图标差异化：◈=资源 / ✦=叙事 / ⚠=风险
- [x] 派遣按钮 `deploy-pulse` 缩放+发光动画（0.6s）
- [x] 出发音效：锯齿波60→100→80Hz + 方波金属撞击泛音
- [x] 资源返回音效：C5→E5→G5 大三和弦上升音阶
- [x] 叙事返回音效：120Hz低频脉冲 + 5Hz调制泛音（人声质感）
- [x] 风险返回音效：300→80Hz下行滑音 + 50Hz撞击低音
- [x] 返回通知横幅：顶部滑入"🔭 勘探队已返回"，6秒自动消失，点击打开面板

**Layer 2 — 有限探索 + 历史记录**
- [x] 每个勘探点只能探索一次（`maxAttempts: 1`）
- [x] 已完成点显示灰色 + ✓ 已完成标记 + 上次结果摘要
- [x] 新增"📋 日志"标签页，按时间倒序显示所有勘探记录
- [x] 日志条目：地点名 + 结果类型 + 周数 + 结果描述 + 派遣成员
- [x] 标签页切换："📍 地点" / "📋 日志"

**Layer 3 — 叙事联动**
- [x] 12个勘探点的叙事结果全部添加 `revealsClue` 字段（关联线索链）
- [x] 揭示线索时日志提示："这次发现让你想起了某份档案……"
- [x] 6个勘探点有 `guardianSpecials`（米莎×2/芬恩×1/洛恩×2/埃塞尔×1/缇卡×1）
- [x] 派遣特定守护者到特定地点触发专属对话（延迟500ms）
- [x] 7条守护者勘探专属对话（缇卡-寂静之城/芬恩-图书馆/米莎-森林+地窖/洛恩-工厂+发射场/埃塞尔-圣殿）

**Layer 4 — 风险后果深化**
- [x] 风险结果触发疲劳系统：被派遣守护者2周内无法再次派遣
- [x] 疲劳守护者显示红色边框 + 禁用选择 + hover提示
- [x] 风险结果50%概率该守护者好感度-1（"你让我去了一个危险的地方"）
- [x] 派遣时排除疲劳守护者，强制玩家做人员调配抉择

**存档兼容性**
- [x] `completedExplorations`/`fatigue`/`explorationLog` 纳入序列化
- [x] 旧存档读档自动初始化为空对象/数组（`|| {}` / `|| []`）

**附带修复**
- [x] 修复 `addLog` 参数顺序错误（3处：`addLog('system', msg)` → `addLog(msg, 'system')`）

#### 验证结果

- 5个JS文件 Node 语法检查通过
- 6个 JSON 文件结构合法
- 54项 ad-hoc 验证全部通过
- 覆盖：函数定义、CSS类、HTML元素、数据字段、音效导出、存档序列化、参数顺序

---

### 2026-07-31 — Bug修复5：标题画面按钮无响应

**问题**：标题画面「🏆 成就」「📖 回顾」「关于」三个按钮点击无效。

**根因**：
1. `showAboutModal()` 使用了未定义的变量 `content`（未从 DOM 获取）
2. `initAchievementsPanel()` 和 `initCodexPanel()` 定义在 `ui.js` 中，但被 `main.js` 调用。由于 `main.js` 先于 `ui.js` 加载，`typeof` 检查返回 `false`，事件监听器从未绑定。

**修复文件**：
| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 修改 | `showAboutModal()` 添加 DOM 查询获取 overlay/title/content/closeBtn |
| `js/main.js` | 修改 | 移除 `typeof` 守卫，直接调用 `initAchievementsPanel()` / `initCodexPanel()` |
| `js/ui.js` | 修改 | `initUI()` 末尾添加 `initAchievementsPanel()` / `initCodexPanel()` 调用 |

**验证结果**：28项全部通过

---

### 2026-07-31 — Bug修复6：ui.js 函数丢失导致新游戏崩溃

**问题**：新建游戏/新周目时报错 `Uncaught ReferenceError: renderVaultTabs is not defined`，导致存储室标签、归档条目、项目列表全部无法渲染。

**根因**：`ui.js` 重写时遗漏了 7 个核心渲染函数，`renderAll()` 调用这些未定义的函数时崩溃。

**修复文件**：
| 文件 | 操作 | 说明 |
|------|------|------|
| `js/ui.js` | 补回 | 恢复 `renderVaultTabs()`、`renderVaultStatus()`、`renderArchiveEntries()`、`updateProjectButton()`、`renderProjectList()`、`getProjectEffectText()`、`getResourceName()` 共 7 个函数 |

**验证结果**：32项全部通过

---

## 已知问题（待修复）

| 问题 | 优先级 | 说明 |
|------|--------|------|
| ~~地表勘探反馈机制弱~~ | ~~中~~ | ✅ 已完成四层增强（视听/有限/叙事/风险） |
| 中期体验空洞 | 中 | 第8-12周之间事件密度不足 |
| 叙事分支未细化 | 低 | 隐藏结局条件过于依赖存储室占比 |

---

*本日志由开发助手与玩家协作维护，每次修改均追加时间戳记录。*

---

## 附录：完整开发进度总结

### 已完成阶段总览

| 阶段 | 日期 | 核心内容 | 状态 | 验证 |
|------|------|----------|------|------|
| MVP-0 | 07-28 | 最小核心循环（发现→归档→资源消耗） | ✅ 完成 | 59项 |
| 模块A+B | 07-28 | 日志系统 + 守护者互动 + 新手引导 | ✅ 完成 | 26项 |
| 第二阶段 | 07-28 | 存档系统 + New Game Plus + 标题画面 | ✅ 完成 | 14项 |
| 路线A | 07-29 | 生存压力（衰减加速） + 跳过回合 | ✅ 完成 | 30项 |
| 路线B | 07-29 | 线索链 + 文明图谱 + 画像 + 隐藏结局 | ✅ 完成 | 65项 |
| 第五阶段 | 07-29 | 5项UX优化（布局/字体/按钮等） | ✅ 完成 | 25项 |
| 第六阶段 | 07-29 | 失败条件 + 周数上限 + 归档确认 | ✅ 完成 | 30项 |
| Bug修复1 | 07-29 | 确认弹窗 + 困局横幅 + 返回标题 | ✅ 完成 | 25项 |
| 子系统C | 07-29 | 地表勘探系统（12个点/派遣/结果） | ✅ 完成 | 73项 |
| 子系统D | 07-29 | 紧急归档 + 终章对话 + 维护项目 + 终局事件 | ✅ 完成 | 64项 |
| Bug修复2 | 07-29 | 标题按钮无响应（initProjects空状态） | ✅ 完成 | 12项 |
| Bug修复3 | 07-29 | 读档空壳化（loadGame未初始化state） | ✅ 完成 | 34项 |
|| 第七章 | 07-30 | 衰败视听系统（Canvas衰败层+Web Audio+静音） | ✅ 完成 | 15项 |
| Bug修复4 | 07-30 | 设置系统 + 音频/归档/勘探/周数多项运行时Bug | ✅ 完成 | 27项 |
| 方向一 | 07-30 | 压力深化（失败放宽+跳过代价+应急协议+项目反馈） | ✅ 完成 | 14项 |
| 子系统E | 07-31 | 地表勘探反馈机制（四层增强：视听/有限/叙事/风险） | ✅ 完成 | 54项 |
| Bug修复5 | 07-31 | 标题画面按钮（关于/成就/回顾）无响应修复 | ✅ 完成 | 28项 |
| Bug修复6 | 07-31 | ui.js 函数丢失导致新游戏崩溃（renderVaultTabs等7函数补回） | ✅ 完成 | 32项 |

### 代码规模

| 类别 | 行数 |
|------|------|
| HTML | ~300 |
| CSS | ~2,470 |
| JS (5 files) | ~5,700+ |
| JSON (7 files) | ~3,600+ |
| **总计** | **~13,000+** |

### 核心系统实现

- 12间存储室 × 63条归档 + 15条NG+条目 = 扩展归档系统
- 5名守护者 × 5级好感度 × 3-5条对话 × NG+轮回对话 + 7条勘探专属对话 = 好感度系统
- 16个数据驱动结局（全收集/守护者专属/vault组合/百分比）
- NG+继承系统：解锁条目 + 记录守护者结局 + 累计统计
- 8个NG+专属事件 + 5个守护者个人事件 = 多周目叙事
- 11个事件 + 10个未来触发器 + 3个终局事件 = 事件系统
- 12个勘探点 × 4种结果 × 4层反馈（视听/有限/叙事/风险）= 地表勘探
- 3个维护项目 = 后期内容
- 5条紧急归档 = 中期压力
- Web Audio API 环境音系统（9种音效）= 视听沉浸
- Canvas衰败视觉层 = 资源状态可视化
- 应急协议系统 = 资源转化抉择
- 35项成就系统 = 长期目标
- 16种结局回顾面板 = 多周目叙事沉淀

### 存档兼容性

- 所有状态字段均有 `||` 默认值保护
- 旧版存档读档后自动填充缺失字段
- `activeProjects`/`completedProjects` 自动初始化为空数组

---
### 待办
- [ ] 中期内容空洞（第16-35周）
- [x] 成就"不跳过"可能无法完成 → 已通过数值平衡解决
- [x] 资源获取手段不足 → 已通过地表残响事件解决

---

## 2026-07-31 数值平衡调整（方案B）

### 问题诊断
- 条目从63膨胀到113（+79%），经济参数从未调整
- 起始介质60，周衰减2 → 第30周归零
- 跳过净收益仅+2介质/周，机会成本严重不对称
- 最大可归档仅~7条（占6%），"不跳过"成就不可能完成

### 修改内容

**1. 衰减参数调整** — `js/game.js` getWeeklyDecay()
- 能源衰减: 3 → 1.5（↓50%）
- 介质衰减: 2 → 0.8（↓60%）
- 环境衰减: 1.5 → 0.5（↓67%）

**2. 跳过恢复量调整** — `js/game.js` skipTurn()
- 能源恢复: +8 → +18（↑125%）
- 介质恢复: +4 → +12（↑200%）
- 环境恢复: +3 → +8（↑167%）

**3. 条目成本调整** — `data/archives.json`
- 原始63条: 能源×0.80, 介质×0.75 → 新平均 E7.0/M13.0
- v2新条目(45条): 能源×0.70, 介质×0.60 → 新平均 E5.6/M9.5
- 紧急条目(5条): 保持不变
- NG+专属(15条): 保持不变

**4. 新增周期事件** — `data/events.json` + `js/game.js`
- 新增"地表残响"事件（event_surface_echo）
- 每4周自动触发（第4/8/12/16/20/24/28/32/36/40/44/48周）
- 效果: +10能源, +15介质
- 在checkRandomEvent()中新增periodic类型事件的优先触发逻辑

### 预期效果
| 场景 | 修改前 | 修改后 |
|------|--------|--------|
| 策略跳过（24+24周） | ~7条 | ~31条 |
| 不跳过（48周全归档） | -5条（不可能） | ~15条（困难但可行） |
| "不跳过"成就 | 不可能完成 | 可完成10+条 |

### 验证
- archives.json: 113条基础条目成本已按比例调整 ✓
- events.json: event_surface_echo 已添加 ✓
- game.js: 衰减参数、跳过恢复量、周期事件逻辑均已修改 ✓



## 2026-07-31 — 第七章：时间轴扩展（v2.0 章节拓展）

**目标**：将游戏时间轴从 20 周扩展至 48 周，解决"半成品感"和"紧迫感不足"问题，为中后期内容提供合理空间。

> **规划文档**：`游戏章节拓展.md`（已新建，含完整设计数据）

#### 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `js/game.js` | 修改 | `MAX_WEEK` 20→48；终章对话触发 15→30周；封印门槛 10→20周；成就条件调整 |
| `js/audio.js` | 修改 | 终局心跳启动 18→36周；加速公式调整 |
| `js/canvas.js` | 修改 | 边缘泛红启动 16→32周；强度公式调整 |
| `js/ui.js` | 修改 | 应急按钮禁用条件 20→48周；进度条阈值 90/70→80/60 |
| `js/main.js` | 修改 | 帮助文本 10周→20周 |
| `data/events.json` | 重写 | 终局事件 18/19/20→42/45/48周；8个随机事件时间窗口调整 |
| `data/archives.json` | 重写 | 63条过期时间 4-8→12-24周；紧急归档 8→16周 |
| `data/projects.json` | 修改 | 可用时间 10/10/12→20/20/24周 |

#### 已实现功能

**时间轴扩展**
- [x] `MAX_WEEK` 常量从 20 周扩展至 48 周
- [x] 终局事件序列从第 18/19/20 周延后至 42/45/48 周
- [x] 终章对话触发从第 15 周延后至 30 周
- [x] 封印门槛从第 10 周延后至 20 周

**视听效果适配**
- [x] 终局心跳从第 18 周延后至 36 周，加速公式调整
- [x] Canvas 边缘泛红从第 16 周延后至 32 周，强度公式调整
- [x] UI 进度条阈值从 90%/70% 调整为 80%/60%

**数据调整**
- [x] 63 条归档条目过期时间全部调整（12-24 周范围）
- [x] 5 条紧急归档解锁时间从第 8 周延后至 16 周
- [x] 8 个随机事件时间窗口调整（匹配新时间轴）
- [x] 3 个维护项目解锁时间从第 10/10/12 周延后至 20/20/24 周

**内容填充**
- [x] 新增 45 条 v2 归档条目（9 章 × 5 条，覆盖第 13-48 周）
- [x] 新增 10 个中后期事件（第 13-48 周完整分布）
- [x] 新增 4 个维护项目（水循环/医疗/通讯/最终封印）
- [x] 新增 6 个中后期勘探点（灰绿之海/尘暴村庄/病变诊所/暴动街区/最终废墟/圣所核心）
- [x] 新增守护者勘探专属对话（11 条）

#### 验证结果

- 全部 JS 文件 Node 语法检查通过
- 所有 JSON 文件结构合法
- 全部 128 条归档条目数据完整
- 全部 18 个随机事件时间窗口合法
- 全部 7 个维护项目配置完整
- 全部 18 个勘探点数据完整
- Ad-hoc 验证脚本：**ALL CHECKS PASSED**

#### 后续计划（概述）

详见 `游戏章节拓展.md`

**已完成**：
- [x] 时间轴扩展至 48 周
- [x] 归档条目 63 → 113 条
- [x] 事件 8 → 18 个
- [x] 项目 3 → 7 个
- [x] 勘探点 12 → 18 个

**待推进**：
- [ ] 数值平衡（经济模型/难度曲线）
- [ ] 新增条目之间的 `relatedArchives` 关联链
- [ ] 新增章节过渡叙事
- [ ] 中期事件与终局评价系统联动
- [ ] 多周目继承内容扩展

---

## 2026-08-01 Bug 记录 & 下一步

### 已知 Bug（标记为暂时无法修复）

| # | 描述 | 状态 |
|---|------|------|
| 1 | 圣所维护项目按钮在资源不足时 disabled，无法点击 | 暂时无法修复 |
| 2 | 「资源不足以归档任何条目」横幅可能不显示 | 暂时无法修复 |
| 3 | 到达第 48 周后封存圣所提示不出现 | 暂时无法修复 |

### 下一步计划

根据上次验证结论（数值平衡已通过），下一步建议：

1. **实际测试**：启动服务器跑一遍 48 周，验证资源曲线体感
2. ~~**内容关联**~~ ✅ 已完成
3. **章节过渡叙事**：补充中期章节之间的过渡事件
4. **多周目继承扩展**：丰富 NG+ 专属内容

---

## 2026-08-01 章节过渡叙事 + 多周目扩展（并行推进）

### 一、章节过渡叙事

新增 **11 个章节过渡事件**（`chapter_transition_02` 到 `chapter_transition_12`），在玩家进入新章节的第一周触发。

| 章节 | 周数 | 标题 | 核心内容 |
|------|------|------|----------|
| 2 | 5-8 | 裂痕初现 | 圣所设备老化，守护者首次讨论"还能撑多久" |
| 3 | 9-12 | 地表恶化 | 勘探队带回坏消息，存储室出现空位 |
| 4 | 13-16 | 裂痕加深 | 中期评估：已归档条目能否代表文明全貌？ |
| 5 | 17-20 | 衰退加速 | 衰减速度加快，系统负荷过载 |
| 6 | 21-24 | 灰绿之海 | 海洋生态崩溃，米莎情绪低落 |
| 7 | 25-28 | 沉默降临 | 地表通讯中断，圣所被彻底隔绝 |
| 8 | 29-32 | 病变蔓延 | 守护者健康出现问题 |
| 9 | 33-36 | 秩序崩塌 | 守护者之间出现真正分歧 |
| 10 | 37-40 | 告别准备 | 讨论"封印后的事"，留下遗言 |
| 11 | 41-44 | 最终倒计时 | 低功耗模式，最后归档冲刺 |
| 12 | 45-48 | 终章 | 封印前的最终抉择 |

**触发机制**：`checkRandomEvent()` 中最优先检测 → `chapter_transition` 类型 → 100% 触发

### 二、多周目扩展

#### 2.1 新增 10 条 NG+ 专属条目

| ID | 标题 | 解锁条件 |
|----|------|----------|
| `arch_ng_previous_keeper_log` | 前代守护者日志 | 周目 ≥2 |
| `arch_ng_sanctuary_7_transmission` | 第七圣所的最后通讯 | 周目 ≥2 |
| `arch_ng_green_ball_log` | 绿色缤球号探测记录 | 周目 ≥3 |
| `arch_ng_tika_dream` | 缇卡的梦境记录 | 缇卡友好+，周目 ≥2 |
| `arch_ng_finn_loop_theory` | 芬恩的循环理论 | 芬恩友好+，周目 ≥3 |
| `arch_ng_misha_other_world` | 米莎的另一个世界 | 米莎友好+，周目 ≥2 |
| `arch_ng_lorn_final_design` | 洛恩的最终设计 | 洛恩友好+，周目 ≥3 |
| `arch_ng_ethel_faith_question` | 埃塞尔的信仰之问 | 埃塞尔友好+，周目 ≥2 |
| `arch_ng_keeper_oath` | 守护者的誓言 | 周目 ≥4 |
| `arch_ng_final_message` | 给下一个轮回的信 | 周目 ≥5 |

#### 2.2 新增 3 个 NG+ 事件

| ID | 标题 | 触发条件 | 效果 |
|----|------|----------|------|
| `ng_plus_ghost_signal` | 幽灵信号 | 周目 ≥3，第 10-20 周 | 解锁第七圣所条目 |
| `ng_plus_time_loop` | 时间循环 | 周目 ≥4，第 15-25 周 | 芬恩循环理论条目 |
| `ng_plus_final_choice` | 超越时间 | 周目 ≥5，第 40-48 周 | **真结局**：打破循环 |

#### 2.3 守护者跨周目记忆对话

每位守护者新增 `memoryDialogues`，在新周目第一周触发：
- 周目 2：「这一周……我好像经历过。也许是错觉吧。」
- 周目 3：「第三次了。但每一次，都有一些不同。」
- 周目 5：「你已经走了这么远。这一次，一定能到达终点。」

#### 2.4 真结局

新增 `true_ending`（优先级 200，最高），解锁条件：
- 周目 ≥5
- 触发 `ng_plus_final_choice` 事件并选择"打破循环"

结局描述：守护者们站在绿草地上，循环被打破，记忆成为新世界的种子。

#### 2.5 新增 5 个成就

| ID | 名称 | 条件 |
|----|------|------|
| `reincarnator` | 轮回者 | 完成 3 个周目 |
| `eternal_keeper` | 永恒守护者 | 完成 5 个周目 |
| `beyond_time` | 超越时间 | 第 5 周目打破循环 |
| `chapter_complete_12` | 终章见证者 | 经历全部 12 个章节过渡 |
| `memory_keeper` | 记忆收集者 | 解锁全部 NG+ 专属条目 |

### 三、代码修改

**`js/game.js`**：
- `checkRandomEvent()`：新增 `chapter_transition` 类型事件优先检测
- `resolveEvent()`：新增 `triggerEnding` 反馈处理（真结局触发）
- `checkHiddenEndings()`：新增真结局判定（优先级最高）
- `getMoodDialogues()`：新增跨周目记忆对话
- `checkAchievements()`：新增 `playthroughs`/`ending`/`chapter`/`unlock_ng_entries` 条件
- `onTimeAdvanced()`：新增章节完成检测
- 新增 `checkChapterCompletion()` 函数

**`js/main.js`**：
- `initGameState()`：新增 `chaptersCompleted` 和 `pendingEnding` 状态字段

### 四、数据变化

| 文件 | 修改前 | 修改后 | 新增 |
|------|--------|--------|------|
| events.json | 22 | 33 | +11 章节过渡 +3 NG+ |
| archives.json | 128 | 138 | +10 NG+ 条目 |
| guardians.json | 5 | 5 | +memoryDialogues |
| achievements.json | 35 | 40 | +5 成就 |
| endings.json | 9 | 10 | +true_ending |

### 五、验证

- ✅ 全部 JSON 文件合法
- ✅ JS 语法检查通过
- ✅ 所有 relatedArchives 引用有效
- ✅ 章节过渡事件触发条件正确（chapterMin/Max 匹配）
- ✅ 22个丢失事件已全部恢复（重新创建）
- ✅ 50周bug已修复：`advanceTime()` 增加上限截断

### 六、已知问题修复

**50周bug**：`advanceTime()` 现在会截断超过 MAX_WEEK(48) 的时间推进。如果玩家在第46周进行耗时3周的勘探，时间只会推进到第48周，而非第49周。

**丢失事件恢复**：22个事件已基于原始设计重新创建。内容可能与原版本有细微差异，但触发条件和核心机制保持一致。

---

## 2026-08-07 跳过回合卡死修复

### 问题

玩家陷入资源归零困局时，stuck-banner 显示跳过链接但点击无效。

### 根因分析

1. `skipTurn()` 第 850 行第一件事就检查 `activeEvent`，如果有活跃事件直接 return false
2. stuck-skip 链接直接调用 `skipTurn()` 而非 `skipTurn(true)`，无法绕过检查
3. stuck-skip 链接用 `setTimeout(50)` 延迟绑定 onclick，DOM 可能被覆盖
4. `triggerScheduledEvent` 不检查 `activeEvent`，会覆盖已有事件

### 修复

1. `skipTurn(forceFromStuck = false)` — 困局跳过绕过 activeEvent 检查
2. stuck-skip 链接直接绑定 onclick（无 setTimeout）
3. `triggerScheduledEvent` 检查 activeEvent，有则推迟到下周

### 验证

- skipTurn() 正常跳过 → ✅
- skipTurn(true) 绕过 activeEvent → ✅
- stuck-skip 直接绑定 → ✅
- triggerScheduledEvent 守卫+推迟 → ✅
- node --check js/game.js → ✅

---

## 2026-08-01 内容关联链加强

### 修改内容

为全部 45 条 v2 条目添加了 `relatedArchives` 关联链，并补充了 base→v2 的双向连接。

**设计原则**：
- **阶段内连接**：同阶段条目形成叙事环（如 arch_v2_001→002→003→001）
- **跨阶段连接**：相邻阶段条目衔接（如 arch_v2_007→arch_v2_011 灰绿之海）
- **跨存储室连接**：~25% 链接跨越不同存储室，鼓励探索
- **守护者主题链**：围绕同一守护者的多条条目串联（如米莎：007→008→015→029）
- **base→v2 双向**：从旧条目指向新条目，让老玩家也能发现新内容

**最终统计**：

| 类别 | 条目数 | 有链接数 | 总链接数 |
|------|--------|----------|----------|
| v2 新条目 | 45 | 45 (100%) | 89 |
| base 原始条目 | 63 | 61 (97%) | 85 |
| emergency 紧急条目 | 5 | 5 (100%) | 5 |
| **合计** | **113** | **111** | **179** |

**验证**：
- 所有 relatedArchives 引用目标均存在 ✓
- JSON 格式合法 ✓
- 跨存储室链接占比 25% ✓

---

*本日志由开发助手与玩家协作维护，每次修改均追加时间戳记录。*

---

## 2026-08-07 Phase 1-4 开发完成

### Phase 1: 经济平衡
- 归档条目介质消耗降低 32% (0.8 * 0.85)
- 后期条目(availableAfter>=20)能源消耗降低 15%
- 项目效果增强：发电机+2→+3, 冷却50%→60%, 农场+3→+4
- 项目持续时间增加
- 勘探收益提升 20%
- 归档率: 23.2% → 26.1%

### Phase 2: 后期内容
- 守护者后期对话: 每个守护者新增 3 条 late_game + 2 条 finale 对话
- 守护者主动事件后期变体: week>=30 触发概率+15%, 新增 3 个后期事件
- 勘探点可用时间设置: 所有旧勘探点 week 1 解锁, 新勘探点按时解锁

### Phase 3: 视听打磨
- 新增 `playProjectComplete()` 音效: 三角波上升音象征完成
- 项目完成时自动播放音效
- 归档音效 `playArchiveChime` 已存在, 项目完成音效已添加

### Phase 4: 扩展内容
- 数据结构验证: 所有 JSON 文件合法
- JS 语法检查: 所有文件通过
- 当前内容: 138条目, 9项目, 18勘探点, 67事件, 17结局

### 已知问题
- ~~归档率仍偏低~~ → 已修复，通过降低条目成本、起始资源、衰减率综合调整
- 项目按钮在资源不足时 disabled（设计如此）
- 部分解锁条目可能不存在（待验证）

---

## 2026-08-08 — P0 稳定性修复 + P1 经济再平衡

### 一、P0 稳定性修复

#### 修复1: triggerGameOver('timeup') 不触发封印
- **问题**: 第48周后显示"时间已至"弹窗但按钮是"返回标题"，无法封印
- **修复**: `triggerGameOver()` 中 `timeup` 原因现在直接调用 `sealSanctuary()`，走完整结局流程
- **影响**: 玩家体验更连贯，48周自动终局时正确触发结局VN+统计

#### 修复2: checkStuckState 误判潜在条目
- **问题**: `potential` 包含了尚未出现的条目（`availableAfter > week`），导致玩家有条目未显示时出现错误提示
- **修复**: `checkStuckState()` 改为只统计 `availableAfter <= week` 且未过期的条目
- **影响**: 困局横幅只在真正可归档条目不足时出现

#### 修复3: 事件 type 字段语义断裂
- **问题**: 30个随机触发事件 `type: null`，代码中 `filter(e => e.type === 'finale')` 无法匹配终局事件
- **修复**: 按 `trigger.type` 自动补全所有事件 type 字段
- **影响**: 事件分发逻辑正确（尤其是终局事件检测）

#### 修复4: processFinaleEvents 错误过滤
- **问题**: `filter(e => e.type === 'finale')` 返回空（因为事件 type 都是 null）
- **修复**: 改为 `filter(e => e.trigger.type === 'finale')`
- **影响**: 3个终局事件（预警/崩溃/最后光芒）正确触发

### 二、P1 经济再平衡

#### 模拟工具
- 编写 Python 模拟器，按游戏参数逐周模拟玩家行为（贪心策略：最便宜优先，无法归档则跳过）
- 基线测试：归档 14/138 (10.1%) → 严重偏低

#### 调整方案
| 参数 | 旧值 | 新值 | 说明 |
|------|------|------|------|
| 起始能源 | 100 | 150 | 提升开局容错 |
| 起始介质 | 60 | 100 | 提升开局容错 |
| 能源衰减 | 1.5/周 | 1.0/周 | 减缓资源枯竭 |
| 介质衰减 | 0.8/周 | 0.5/周 | 减缓资源枯竭 |
| 条目能源成本 | 平均6.3 | 平均5.3 (×0.85) | 整体降低 |
| 条目介质成本 | 平均8.0 | 平均6.0 (×0.75) | 整体降低 |
| 能源上限 | 100 | 150 | 匹配提升后的起始值 |
| 介质上限 | 60 | 150 | 匹配提升后的起始值 |

#### 验证结果
- 归档率: 10.1% → 29.7% (41/138条)
- 跳过次数: 18次 → 7次
- 目标: 28-35% → ✅ 达标

### 三、诊断确认：归档速度
- **结论**: "归档速度"非代码属性，仅出现在事件文本描述中
- **实际**: 归档始终是每周1条（回合制），由玩家点击"录入归档"触发
- **理论上限**: 48周最多归档48条（资源充足且有可用条目时）

### 四、修改文件清单

| 文件 | 修改 |
|------|------|
| `js/game.js` | triggerGameOver/timeup → sealSanctuary; checkStuckState 过滤逻辑; processFinaleEvents 过滤逻辑; 衰减公式; 跳过恢复上限 |
| `js/main.js` | 起始资源 100/60 → 150/100 |
| `data/archives.json` | 全部条目 cost ×0.85/×0.75 |
| `data/events.json` | 补全30个事件 type 字段 |

### 五、验证
- 全部 JSON 文件解析合法
- 全部 JS 文件 node --check 通过
- 经济模拟: 29.7% 归档率 (目标28-35%)
- 事件 type 覆盖率: 67/67 (100%)

---

## v1.6.2 守护者事件线与归档扩展 (2026-08-08)

### 修复内容

#### 1. 立即归档机制 (P2)
- **链式归档奖励**：所有关联条目（≥2个）全部归档时，自动获得1次立即归档机会
- **食物兑换**：应急协议面板新增「食物换归档」按钮（30食物 = 1次机会）
- **立即归档逻辑**：
  - 消耗1次机会，不消耗介质，不推进时间
  - 仍需消耗能源 + 满足存储室容量限制
  - 优先用于稀缺条目或链式奖励

#### 2. 守护者个人事件线 (P1)
- **数据驱动**：`data/guardian_events.json` 新增5个守护者专属事件
  - 缇卡：记忆之歌（好感≥友好时，第8周后15%概率触发）
  - 芬恩：未完之页（第12周30%概率触发）
  - 米莎：意外的幼苗（勘探完成后40%概率触发）
  - 洛恩：引擎室的独白（第20周25%概率触发）
  - 埃塞尔：夜祷（第25周20%概率触发）
- **事件触发优先级**：守护者个人事件 > 章节过渡 > 周期性事件 > 普通随机事件
- **事件效果**：好感度变化 + 新条目解锁

#### 3. 结局演出增强 (P1)
- **入场动画**：结局页渐入 + 内容块延迟滑入
- **标题光晕**：琥珀色text-shadow
- **统计数字弹跳出现**：3个统计项依次pop动画
- **封印音效**：低沉庄严钟声 + 泛音列

#### 4. 音效反馈精细化 (P1)
- **新增音效函数**：
  - `playChainComplete()` — 链式完成：C大调琶音
  - `playGuardianEventTrigger()` — 守护者事件：G大调和弦
  - `playSealSound()` — 封印：125Hz低频钟声
- **音效触发时机**：
  - 守护者事件触发时 → 温暖和弦
  - 链式归档完成时 → 上升琶音
  - 封印圣所时 → 庄严钟声
  - 立即归档时 → 清脆双音（已有）
  - 守护者好感提升时 → 叙事音效

### 验证
- ✅ 全部 JS 语法检查通过
- ✅ 守护者事件 revealArchive ID 全部匹配现有条目
- ✅ 食物兑换逻辑 UI 与应急协议面板集成
- ✅ 立即归档按钮在确认弹窗中条件显示

### 新增文件
- `data/guardian_events.json` — 守护者个人事件池

---

## v1.6.1 UI/UX 优化 (2026-08-08)

### 修复内容

#### P0 紧急修复
1. **守护者卡片排列修复** — `#dispatch-guardians` 从 `display:flex` 改为 `flex-direction:column`，解决食物消耗与网格水平排列导致的单列问题
2. **滚动提示位置修正** — 将滚动提示从 `#explore-dispatch::before`（选择后才可见）移至 `#explore-list-container::before`（地点列表开头），配合 JS 滚动监听实现滚动后淡出

#### P1 体验优化
3. **守护者全局视图** — 在守护者面板底部添加 5 名守护者心情概览栏，点击可切换主守护者
4. **疲劳状态反馈增强** — 疲劳守护者显示红色"疲劳"角标 + hover 显示剩余恢复周数

#### P2 功能补充
5. **食物预警机制** — 食物 ≤20 黄色脉冲警告，≤10 红色紧急脉冲
6. **守护者详情面板** — 新增"详情"按钮，展示技能/关系等级/职责信息

### 验证结果
- 全部 JS 文件 node --check 通过
- 全部 HTTP 200 可访问

---

## 2026-08-09 — v1.6.3 测试版稳定性与打磨

### 一、P0 稳定性兜底

#### 1. onTimeAdvanced 异常隔离
- **问题**：`onTimeAdvanced()` 包含多个子系统调用（衰减、士气、调度、事件等），任何一个抛出异常都会导致后续 `renderAll()` 不执行，UI 冻结。
- **修复**：将整个函数体包裹在 try-catch 中，异常时打印到 console 但不中断流程。
- **影响**：极端情况下某个子系统崩溃时，玩家仍能看到 UI 更新而非白屏。

#### 2. unhandledrejection 全局监听
- **问题**：JSON 文件加载失败等 Promise 异常会导致启动画面卡死，无任何提示。
- **修复**：在 `main.js` 顶部添加 `window.addEventListener('unhandledrejection', ...)`，在启动画面显示错误信息。
- **影响**：加载失败时玩家能看到"加载失败: ..."而非无限等待。

### 二、P1 性能防护

#### 3. Canvas 粒子上限
- **问题**：`particles` 数组无限增长（每 8 帧 push 一个），长时间游玩后内存持续增大。
- **修复**：`addParticle()` 开头添加 `if (particles.length >= 200) return;`。
- **同步**：`floatingSymbols` 数组添加 `>= 80` 上限。

### 三、P2 数据完整性

#### 4. revealArchive 引用校验
- **执行**：运行脚本验证 `guardian_events.json`、`events.json` 中所有 `revealArchive` 字段指向的 ID 存在于 `archives.json`。
- **结果**：0 个断链，全部引用有效。

### 四、P3 UX 打磨

#### 5. 底部按钮统一音效
- **问题**：`func-bar` 按钮（跳过/图谱/存档/帮助/关于/标题）点击无音效反馈。
- **修复**：
  - `audio.js` 新增 `playButtonClick()` — 短促清脆的正弦波下滑音（1000→600Hz, 80ms）
  - `game.js` 中 `initFuncBar()` 统一为所有底部按钮绑定音效
  - 移除重复的 `initSkipTurn()` 监听器（已整合到 `initFuncBar`）
  - 移除重复的 `initCivilizationAtlas()` 中图谱按钮监听器
- **附加修复**：`initSaveSystem()` 中存档按钮改为调用 `openSaveScreen('save')`（之前直接调用 `saveGame()` 跳过槽位选择）

#### 6. 设置面板主题切换
- **问题**：设置面板缺少主题切换入口。
- **修复**：
  - `index.html` 设置面板新增"主题"选项（暗色/亮色按钮）
  - 内联 `<script>` 初始化按钮点击事件 + 当前主题高亮
  - `css/main.css` 新增 `.settings-theme-row` / `.settings-theme-btn` / `.active` 样式
  - 主题切换时播放按钮音效

### 五、P5 测试版特需

#### 7. 存档导出/导入
- **功能**：玩家可将存档导出为 Base64 文本，用于分享/备份/调试。
- **实现**：
  - `game.js` 新增 `exportSaveToClipboard(slot)` / `importSaveFromClipboard()` / `initExportImport()`
  - `index.html` 存档面板底部新增"📤 导出存档"和"📥 导入存档"按钮
  - `css/main.css` 新增 `#save-export-import` / `.save-action-btn` 样式
  - `main.js` 初始化链添加 `initExportImport()`
  - 使用 `btoa/unescape/encodeURIComponent` 编码，兼容中文

#### 8. 结局页 NG+ 快捷按钮
- **问题**：玩家看完结局后必须"返回标题 → 新建游戏"，路径过长。
- **修复**：
  - `index.html` 结局页脚新增"开始 NG+"按钮（默认隐藏）
  - `game.js` `showEndingSummaryPage()` 中显示该按钮，点击直接调用 `startNewGame(slot, true)`
  - `css/main.css` 新增 `#ending-ng-btn` 样式（绿色，与返回按钮并排）

### 六、验证

- 全部 JS 文件 Node 语法检查通过 ✓
- 全部 HTTP 200 可访问 ✓
- 启动画面 HTML 包含新增元素检查 ✓

### 七、代码规模变化

| 文件 | 旧行数 | 新行数 | 变化 |
|------|--------|--------|------|
| game.js | 5124 | 5264 | +140 |
| main.js | 718 | 737 | +19 |
| canvas.js | 1087 | 1093 | +6 |
| audio.js | 1092 | 1112 | +20 |
| css | 4667 | 4741 | +74 |
| index.html | 522 | 572 | +50 |
| **总计** | **15191** | **15329** | **+138** |

---

## 当前状态

**版本**：v1.6 UI/UX 刷新 + 紧急归档协议
**代码规模**：~12,000+ 行（不含数据 JSON）
**验证通过**：JS 语法 ✓、HTTP 200 ✓、git 提交 ✓

---

### 2026-08-24 — 紧急归档协议 + UI/UX 刷新

**目标**：增加有代价的批量归档机制 + 多项体验优化

#### 1. 紧急归档协议（P0 机制）
- **机制**：第30周后解锁，一回合最多归档3条，不消耗时间
- **代价设计**：环境-10、全体守护者心情-2、下周衰减+20%
- **取消保护**：未归档时取消退回全部代价
- **实现文件**：
  - `js/game-archive.js`：`enterBatchArchiveMode()` / `exitBatchArchiveMode()` / `finishBatchArchive()` 三个核心函数
  - `js/game.js`：`getWeeklyDecay()` 中应用 `nextWeekDecayPenalty` 并消耗
  - `js/main.js`：`initGameState()` 新增 `batchArchiveMode` / `batchArchiveCount` / `batchArchiveUsedThisRun` / `nextWeekDecayPenalty` 字段
  - `index.html`：标题栏新增"🚨 紧急归档 (0/30)"按钮
  - `css/main.css`：`.panel-title-btn.active`（危险红色）+ `.locked`（灰色）样式

#### 2. 批量归档退出确认（P1 UX）
- **问题**：原点击即退出，无确认易误操作
- **修复**：`js/ui.js` 新增 `showBatchExitConfirm()` 弹窗
- **逻辑区分**：
  - 已归档 0 条：提示"退回代价"
  - 已归档 > 0 条：提示"推进1周、代价不退"

#### 3. 衰减惩罚可视化（P1 UX）
- **问题**：玩家无法直观感知"下周衰减+20%"的代价
- **修复**：
  - `css/main.css`：`#resource-panel.decay-penalty` 类添加琥珀色脉冲边框（`color-mix` 适配主题）
  - `js/ui.js`：`renderResources()` 中检测 `nextWeekDecayPenalty` 并添加类
  - `js/ui.js`：`getResourceBreakdown()` 中悬停提示新增"紧急归档代价"条目

#### 4. Bug 修复
- **ARCHIVES 未定义**：`game-archive.js` `getAvailableArchives()` 中 `ARCHIVES.filter()` 从未定义，改为 `MemorySanctuary.data.archives.filter()`，修复紧急归档按钮点击崩溃
- **ensureState() 死代码**：移除未调用的函数
- **.gitignore 清理**：新增 `balance-report*.txt` / `HANDOFF.md` / `js/balance-sim*.js`

#### 5. 代码规模变化

| 文件 | 旧行数 | 新行数 | 变化 |
|------|--------|--------|------|
| game-archive.js | ~600 | 770 | +170 |
| game.js | ~2417 | 2480 | +63 |
| main.js | ~777 | 868 | +91 |
| ui.js | ~1590 | 1684 | +94 |
| css/main.css | ~5524 | 5540 | +16 |
| index.html | ~692 | 716 | +24 |
| **总计** | **~11600** | **~12058** | **+458** |

#### 6. git 记录

```
4140e1e feat: 紧急归档协议 + DLC 框架 + UI/UX 刷新
```

#### 7. 后续待办

- [x] 批量归档退出二次确认
- [x] 衰减惩罚 UI 预警
- [x] 食物系统/勘探重平衡
- [ ] DLC 模块内容填充（ascension / greenOrb）

---

### 2026-08-24 — 食物系统/勘探重平衡

**目标**：将饥饿结局率从 30~54% 降至 5~15%

#### 1. 季节系统温和化
- 旧：`[0, +1, -1, -2]`（每48周净-2）
- 新：`[0, +2, 0, -1]`（每48周净+1）
- 修改文件：`js/game.js` SEASONS 常量 + `js/balance-sim-v2.js` SEASONAL_FOOD_MOD

#### 2. 新增4个食物产出勘探点

| 名称 | 解锁周 | 食物产出 | 消耗 | 技能要求 |
|------|--------|---------|------|---------|
| 废弃果园 | 1 | 16 (40%) | 5 | 无 |
| 地下菌窟 | 15 | 20 (40%) | 8 | 生态+生存 |
| 休眠保险库 | 18 | 18 (40%) | 8 | 工程+生存 |
| 温室废墟 | 37 | 22 (40%) | 8 | 生态+生存 |

- 食物产出点从 4 → 8（占比 18% → 31%）
- 新增 JSON 条目 + 守护者特殊互动（米莎×3）

#### 3. 模拟结果对比

| 策略 | 旧饥饿率 | 新饥饿率 | 旧周数 | 新周数 |
|------|---------|---------|--------|--------|
| 随机 | 54.1% | **12.9%** | 41.4 | 44.3 |
| 能源优先 | 30.0% | **3.8%** | 45.9 | 47.3 |
| 总量优先 | 34.7% | **2.9%** | 46.1 | 47.3 |
| 到期优先 | 49.5% | **12.6%** | 39.1 | 42.6 |
| 均衡 | 45.4% | **5.0%** | 45.0 | 46.9 |

---

*本日志由开发助手与玩家协作维护，每次修改均追加时间戳记录。*
