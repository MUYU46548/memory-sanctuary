# 记忆圣所 开发日志

> 项目名称：记忆圣所 (Memory Sanctuary / Nar-Sil-Veth)  
> 技术栈：HTML5 + CSS3 + Canvas 2D + Vanilla JavaScript  
> 目标平台：浏览器（桌面与平板）  
> 端口：8099  

---

## 项目概述

玩家扮演"静默之翼"守护者，在行星级灾难中管理一座深埋地下的记忆圣所。时间有限、资源递减，玩家必须在终结到来前决定保存文明的哪些碎片。每一段被归档的记忆都揭示萨拉达斯文明的一个切面——而玩家的选择决定了后世"看到"的是怎样的文明。

**核心循环**：发现文明碎片 → 分配到十二间存储室 → 消耗能源录入 → 维护圣所环境 → 应对突发事件 → 直至最终封存

---

## 开发进度

### 2026-07-28 — 原型验证阶段（MVP-0）

**目标**：跑通"发现→分配→录入→归档"最小循环

#### 新增文件

| 文件 | 说明 |
|------|------|
| `index.html` | 页面骨架：顶栏 + Canvas主厅 + 右侧面板 + 底部日志 |
| `css/main.css` | 暗调琥珀色美学、档案目录风格 |
| `js/main.js` | 入口初始化、数据加载 |
| `js/game.js` | 资源管理、归档流程、时间系统、日志弹窗 |
| `js/ui.js` | 资源面板、存储室状态、归档条目渲染 |
| `js/canvas.js` | 圣所主厅：穹顶 + 歌者之座 + 声波可视化 |
| `data/archives.json` | 3条测试归档条目 |
| `data/vaults.json` | 1间测试存储室 |

#### 已实现功能

- [x] Canvas绘制圣所主厅（静态背景 + 动态声波环）
- [x] 资源面板（能源/介质/环境三数值实时显示）
- [x] 1间存储室状态显示（容量条）
- [x] 3条归档条目列表（含成本与过期时间）
- [x] 录入按钮（消耗资源 → 归档 → 反馈）
- [x] 归档完成弹窗（含守护者反应）
- [x] 底部日志系统（系统/守护者/成功三种类型）
- [x] 时间推进与条目过期机制
- [x] 资源不足/已归档/已消失状态处理

#### 验证结果

- 全部文件HTTP 200可访问
- 4个JS文件Node语法检查通过
- 20项ad-hoc验证全部通过

---

### 2026-07-28 — MVP扩展阶段

**目标**：扩展至3间存储室、12条条目、5名守护者、5个突发事件

#### 新增/修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/archives.json` | 重写 | 扩展至12条条目（语言4条/历史4条/灾难4条） |
| `data/vaults.json` | 重写 | 扩展至3间存储室（语言/历史/灾难） |
| `data/guardians.json` | 新增 | 5名守护者数据（含4种对话类型） |
| `data/events.json` | 新增 | 5个突发事件（含选择分支与效果） |
| `index.html` | 修改 | 添加主题切换按钮、守护者面板、事件面板、存储室标签栏 |
| `css/main.css` | 重写 | 添加暗色/常规双主题、自定义滚动条、新组件样式 |
| `js/main.js` | 重写 | 添加主题初始化、加载守护者/事件数据 |
| `js/game.js` | 重写 | 添加守护者系统、事件系统、存储室切换 |
| `js/ui.js` | 重写 | 添加存储室标签栏渲染、守护者面板 |
| `js/canvas.js` | 重写 | 添加多闸门绘制、主题适配 |

#### 已实现功能

- [x] 3间存储室（语言与语法/历史编年/灾难纪实）
- [x] 12条归档条目（每间4条，含守护者反应）
- [x] 5名守护者（缇卡/芬恩/米莎/洛恩/埃塞尔）
- [x] 守护者对话系统（idle/archive/lowEnergy/event四种类型）
- [x] 5个突发事件（光雨渗透/能源波动/勘探发现/守护者分歧/设备故障）
- [x] 暗色/常规主题切换（LocalStorage持久化）
- [x] 自定义滚动条（Webkit + Firefox双兼容）
- [x] 存储室标签栏切换
- [x] 守护者面板（头像+姓名+角色+对话）
- [x] 事件通知横幅（含选择按钮）
- [x] 端口更换至8099

#### 验证结果

- 21项ad-hoc验证全部通过
- 覆盖：文件结构、JSON有效性、JS语法、HTML引用、CSS完整性、逻辑一致性

---

## 当前项目结构

```
memory-sanctuary/
├── index.html                  # 入口页面
├── css/
│   └── main.css                # 全局样式（双主题+滚动条）
├── js/
│   ├── main.js                 # 入口初始化+主题+数据加载
│   ├── game.js                 # 核心逻辑（资源/归档/时间/守护者/事件）
│   ├── ui.js                   # UI渲染（资源/标签/条目）
│   └── canvas.js               # 圣所主厅Canvas绘制
├── data/
│   ├── archives.json           # 12条归档条目
│   ├── vaults.json             # 3间存储室
│   ├── guardians.json          # 5名守护者
│   └── events.json             # 5个突发事件
└── README.md                   # 项目说明
```

---

## 代码统计

| 类型 | 文件数 | 约总行数 |
|------|--------|----------|
| HTML | 1 | 110 |
| CSS | 1 | 430 |
| JavaScript | 4 | 950 |
| JSON数据 | 4 | 280 |
| **合计** | **10** | **~1770** |

---

## 已知问题（待修复）

| 问题 | 优先级 | 说明 |
|------|--------|------|
| 内容单调，缺乏操作指引 | 中 | 需要场景切换、角色互动、新手引导 |
| 底部日志窗口过窄且居下 | 中 | 建议改为按钮入口+展开/收缩+提示标记 |

---

## 下一步计划

### 短期（MVP完善）

- [ ] 场景切换：不同存储室显示不同Canvas场景
- [ ] 角色互动：点击守护者触发对话/事件
- [ ] 日志系统改造：按钮入口 + 展开/收缩 + 新消息提示
- [ ] 新手引导：首次操作高亮提示

### 中期（完整版）

- [ ] 扩展至12间存储室
- [ ] 增加归档条目至60-80条
- [ ] 守护者关系网系统
- [ ] 地表勘探系统
- [ ] 存档系统（LocalStorage）
- [ ] 多周目继承

### 长期（打磨）

- [ ] 音频系统（Web Audio API）
- [ ] 完整框架叙事
- [ ] 数值平衡
- [ ] 性能优化
- [ ] 部署至GitHub Pages

---

## 参考游戏

| 游戏 | 借鉴要素 |
|------|----------|
| What Remains of Edith Finch | 碎片化叙事 |
| Disco Elysium | 文字驱动、内心叙事 |
| Frostpunk | 末日资源管理、道德抉择 |
| This War of Mine | 生存压力、人员管理 |
| The Return of the Obra Dinn | 考古推理、碎片拼凑 |
| Heaven's Vault | 考古语言学、叙事驱动 |

---

*本日志由开发助手与玩家协作维护，每次修改均追加时间戳记录。*
