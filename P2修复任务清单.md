# 记忆圣所 — P2 修复任务清单

> **基线版本**：v0.2.4（2026-08-31 P1 全量修复后）  
> **执行前必读**：`开发日志2.md`（根目录）+ `AGENTS.md`（红线约束）  
> **验证命令**：`node -c js/*.js` 必须全过；`python3 -c "import json; json.load(open('data/xxx.json'))"` 必须全过

---

## 🔴 第1组：逻辑账目（7项）

### T1-1：`resetConsecutiveSkips` 零调用

**现象**：`js/game.js:1272` 定义了 `resetConsecutiveSkips()`，但全库仅有定义，无任何调用点。  
**影响**：连续跳过周数在跳过→归档时不会重置，导致跳过惩罚累加。  
**修复**：在 `archiveEntry()`（`js/game-archive.js` 归档成功路径）和 `completeRitualArchive()` / `quickArchive()` 等归档完成入口调用 `resetConsecutiveSkips()`。  
**验证**：跳过2周后归档，`state.consecutiveSkips` 应归零。

---

### T1-2：饥饿惩罚误清零疲劳

**现象**：`checkFoodAbundancePenalty()`（`js/game.js:801`）在食物归零时可能触发某些副作用，但更关键的是——食物归零时的饥饿逻辑（`js/game.js` 搜索 `food <= 0` 相关）与疲劳系统交互时，可能误清 `exploration.fatigue`。  
**影响**：食物归零后守护者疲劳被意外清空，可立即再次勘探。  
**修复**：审查食物归零分支，确保不触碰 `state.exploration.fatigue`。  
**验证**：食物归零后，已疲劳的守护者应仍显示疲劳状态。

---

### T1-3：勘探疲劳 off-by-one

**现象**：`js/game-exploration.js:605`：`fatigue[gid] = week + fatigueWeeks`。若 `fatigueWeeks=1`，当前第10周，则疲劳到第11周。但判定是否疲劳的逻辑是 `fatigue[gid] > state.week`，即第10周仍可部署（10 > 10 为 false），第11周才疲劳——实际只疲劳了1周（11-10），但 `week + 1 = 11`，第10周可部署说明第10周**没有疲劳**，第11周才疲劳。看起来 off-by-one 在边界：如果 `fatigueWeeks=1` 意味着"疲劳1周"，那第10周（当前周）是否应疲劳？  
**影响**：当前实现是"下周起疲劳"，可能是有意设计，但需确认是否符合"返回后疲劳N周"的语义。  
**修复**：若语义应为"包含当前周在内的N周"，改为 `fatigue[gid] = week + fatigueWeeks - 1`；若当前行为有意（下周起算），则关闭此项并更新日志说明。  
**验证**：`fatigueWeeks=1` 部署后，下周应无法再部署同一守护者。

---

### T1-4：紧急归档扣费口径不一可净赚环境

**现象**：`emergency_archive` 应急协议（`js/game.js:2361`）的 `execute` 只设置 `emergencyArchiveActive=true` 和 `emergencyArchiveUsed`，**不扣资源**。  
在归档入口（`js/game-archive.js`），`emergencyArchiveActive` 时介质消耗=0，能源加倍。  
但如果有其他资源获取路径（如事件奖励 + 应急协议组合），可能净赚。  
**影响**：理论上可以通过"开应急→归档高能耗条目→关应急"循环规避介质。  
**修复**：`execute` 中应增加环境稳定消耗（如 `-10` 环境）作为代价，或确保 `emergencyArchiveActive` 归档时的能源加倍成本足够高。  
**验证**：开启应急归档一条目后，资源总量应减少而非增加。

---

### T1-5：多周推进只按 1 周结算

**现象**：`advanceTime(weeks)`（`js/game.js:78`）和 `onTimeAdvanced(weeks)` 已支持 `weeks` 参数，`applyWeeklyDecay(weeks)` 也乘了 `weeks`。  
但需确认勘探返回（`advanceTime(expData.duration)`）和事件时间推进（`advanceTime(choice.effect.time)`）传入的 `weeks` 值是否正确。  
**影响**：如果某处 `advanceTime` 传入的值被硬编码为1，多周勘探会只结算1周衰减。  
**修复**：审查所有 `advanceTime()` 调用点，确保传入正确的 `duration` 值。  
**验证**：派遣守护者勘探2周，应消耗2周衰减。

---

### T1-6：`playthroughCount` 开局即 +1

**现象**：`js/game-save.js:377` 有 `ngData.playthroughCount++`，在 `startNGPlus()` 中调用。  
但需确认是否存在某处在新游戏（非 NG+）时也调用了递增。  
**影响**：首次游玩即标记为第2周目，稀释真结局门槛（需5周目）。  
**修复**：确认 `playthroughCount++` 仅在 `startNGPlus()` 中调用，新游戏不递增。  
**验证**：首次新游戏，`ngData.playthroughCount` 应为0。

---

### T1-7：导入存档科技清零

**现象**：`js/game-save.js:747` 的 `sanitizeImportedSave()` 未列出 `techUnlocked` 字段。  
但 `loadGame()` 中（`js/game-save.js:210`）有 `state.techUnlocked = [...(saveData.state.techUnlocked || [])]`。  
然而 `sanitizeImportedSave` 的白名单中没有 `techUnlocked`，意味着导入存档时该字段被丢弃。  
**影响**：导入旧档后已解锁科技丢失。  
**修复**：在 `sanitizeImportedSave` 的 `cleanState` 中添加 `techUnlocked: arr(s.techUnlocked).filter(x => typeof x === 'string')`。  
**验证**：导入一个已解锁科技的存档，`state.techUnlocked` 应保留。

---

## 🟡 第2组：数据引用（5项）

### T2-1：勘探 `revealsClue` 中 `arc_*` 应为 `arch_*`

**现象**：`data/explorations.json` 中至少5处 `revealsClue` 使用 `arc_language_01`、`arc_ecology_01`、`arc_astronomy_01`、`arc_orchard_dead`、`arc_medicine_01`。  
而 `data/archives.json` 中的条目 ID 前缀是 `arch_`。  
**影响**：勘探揭示的线索条目 ID 与归档条目 ID 不匹配，线索链断裂。  
**修复**：将 `data/explorations.json` 中所有 `revealsClue` 的 `arc_` 前缀改为 `arch_`（建议全局搜索替换，确认无遗漏）。  
**验证**：`grep -n '"arc_' data/explorations.json` 应返回0结果。

---

### T2-2：`misha_seedling` 引用不存在勘探点 `exp_surface_01`

**现象**：`data/guardian_events.json:80` 中 `explorationId: "exp_surface_01"`。  
搜索 `data/explorations.json` 无此 ID。  
**影响**：米莎的 `misha_seedling` 守护者事件永不触发。  
**修复**：将 `explorationId` 改为 `data/explorations.json` 中存在的勘探点 ID（如 `exp_forest_02` 枯萎之森，与植物叙事吻合）。  
**验证**：确认新 ID 在 `data/explorations.json` 中存在。

---

### T2-3：`scheduled_misha_discovery` 死事件

**现象**：`data/events.json:1644` 的 `scheduled_misha_discovery` 存在，但需检查 `checkRandomEvent` / `checkScheduledEvent` 是否能正确匹配到它。  
若其 `trigger.type` 为 `scheduled` 但调度逻辑不处理该类型，则为死事件。  
**影响**：事件永不触发。  
**修复**：确认调度逻辑覆盖 `trigger.type=scheduled`；若已覆盖则关闭此项，否则修复调度器或删除该事件。  
**验证**：在游戏第12-20周期间应能触发该事件。

---

### T2-4：`conflict_choice` 结局唯一无 VN 场景

**现象**：`data/endings.json:226` 的 `conflict_choice` 结局存在，但 `js/game-ending.js` 和 `js/vn.js` 中未找到对应的 VN 场景引用。  
**影响**：该结局触发时无视觉小说演出，体验断裂。  
**修复**：为 `conflict_choice` 添加 VN 场景（`data/scenes.json` 或 `data/ending_scenes.json` 中新增对应场景，并在 `game-ending.js` 中调用）。  
**验证**：达成 `conflict_choice` 结局时应进入 VN 场景。

---

### T2-5：`exp_bot_depot` p=0 空 message

**现象**：`data/explorations.json:1209` 的 `exp_bot_depot` 最后一条 outcome `probability: 0` 且 `message: ""`。  
概率合计 = 0.4+0.35+0.25+0 = 1.0（日志写1.02是旧值，现为1.0）。  
`probability: 0` 的条目永远不会触发，但保留空 message 是防御性写法。  
**影响**：无实际 bug，但代码可读性差。  
**修复**：删除该 `probability: 0` 的空占位 outcome，或改为 `probability: 0.05` 并补全 message。  
**验证**：所有 outcome 概率合计 = 1.0，无 message 为空的条目。

---

## 🟠 第3组：UI / 主题（6项）

### T3-1：`--explore-green` 亮色对比度 ~2.7:1

**现象**：`css/main.css:73` light 主题 `--explore-green: #3a8a5a`，在白色背景上对比度约2.7:1，未达 WCAG AA（4.5:1）。  
**影响**：勘探面板中绿色边框/文字在浅色主题下难以辨认。  
**修复**：将 light 主题的 `--explore-green` 改为更深的值（如 `#2d6b46` 或 `#1f7d3d`），使对比度 ≥ 4.5:1。  
**验证**：用 WebAIM 对比度检查工具验证 light 主题下 `--explore-green` 在 `--bg-panel`（`#faf7f2`）上的对比度。

---

### T3-2：结算页未过 `esc()`

**现象**：结局/结算页面的文本内容（如 `ending.description`、结局场景文字）可能包含 HTML 特殊字符，若未过 `esc()` 直接 `innerHTML`，存在 XSS 风险或渲染异常。  
**影响**：含 `<`、`>`、`&` 的结局文本可能渲染错误。  
**修复**：审查 `game-ending.js` 和 `vn.js` 中所有 `innerHTML` 赋值，对用户可控数据（JSON 文本）使用 `esc()` 转义。  
**验证**：在结局文本中插入 `<script>test</script>`，应被转义为纯文本显示。

---

### T3-3：成就 Toast 连续触发被旧定时器提前隐藏

**现象**：`js/ui.js:1664` 的 `showAchievementToast()` 使用 `setTimeout(3000ms)` 自动隐藏，但未保存 timer ID。若3秒内再次触发新成就，旧定时器仍会在3秒时隐藏新 toast。  
**影响**：快速连续解锁成就时，后面的成就 toast 被提前隐藏。  
**修复**：用模块级变量存储 timer ID，新触发时 `clearTimeout` 旧定时器。  
**验证**：快速解锁2个成就，第二个应完整显示3秒。

---

### T3-4：双主题若干死规则 / 内联 hex

**现象**：`css/main.css` 中 `:root` 和 `[data-theme="light"]` 使用了内联 hex 值（如 `--bg-deep: #0a0a0f`），这是合法的 CSS 变量定义，但需检查是否有非变量的内联 hex 直接在属性中（如 `color: #ff0000`）。  
**影响**：可能存在少量硬编码颜色未被主题覆盖。  
**修复**：搜索 `css/main.css` 中非变量形式的 hex 值（排除变量定义行），替换为对应 CSS 变量。  
**验证**：`grep -n '#[0-9a-f]\{3,6\}' css/main.css | grep -v '--'` 应返回空或仅保留变量定义。

---

### T3-5：字体 8 秒超时后不应用

**现象**：`js/font-loader.js:45` 有 8000ms 超时，超时后 fallback 到系统字体，但可能未设置 `document.body` 的 `font-family` 为 fallback。  
**影响**：字体加载超时后，界面显示为浏览器默认字体而非预期的 fallback。  
**修复**：在超时回调中添加 `document.body.style.fontFamily = '"WenKai Fallback", "LXGW WenKai", sans-serif'`。  
**验证**：模拟字体加载失败（网络限速），超时后应显示可读的 fallback 字体。

---

### T3-6：`#week-value` 与 `project-btn` 双重绑定双触发

**现象**：`js/ui.js:653` 中 `#week-value` 绑定了 click 事件（显示周数说明）。  
`js/game.js:2214` 中 `#project-btn` 绑定了 click 事件。  
这是两个不同元素，不存在双重绑定。但需确认是否有其他元素同时绑定了 `#week-value` 或 `#project-btn`。  
**影响**：可能是误报，或旧代码遗留。  
**修复**：搜索确认 `#week-value` 和 `project-btn` 各只有一处 click 绑定。若有多处，合并为单一绑定。  
**验证**：点击周数显示日志，点击项目按钮打开面板，各自独立工作。

---

## 🔵 第4组：文案 / 版本（6项）

### T4-1：机器人数值文案 4 处过期

**现象**：  
- `js/game.js:1028`：`"每台减少 10% 衰减并抑制腐败侵蚀，每回合消耗 1 能源"`  
- `js/game-projects.js:156`：`"每台减少 10% 衰减，每回合消耗 2 能源"`  
- `js/ui.js:1262`：显示维护成本 `◈${maintenanceCost}`（应正确计算）  

**实际数值**（`js/game.js:868-870`）：  
- 衰减减免：18%/台  
- 腐败抑制：18%/台  
- 维护消耗：0.75 能源/台/周（`ENERGY_BOT_MAINTENANCE`）

**修复**：将过期文案更新为：  
- `js/game.js:1028` → `"每台减少 18% 衰减并抑制腐败侵蚀，每回合消耗 0.75 能源。"`  
- `js/game-projects.js:156` → `"每台减少 18% 衰减，每回合消耗 0.75 能源。"`  
- 检查 `js/ui.js:1262` 中 `maintenanceCost` 计算是否基于 `0.75`。  

**验证**：游戏内机器人面板显示的数值应与 `ENGINEERING_BOTS_CONFIG` 一致。

---

### T4-2：12 个成就缺 `category`

**现象**：以下12个成就缺少 `category` 字段：  
`reincarnator`, `eternal_keeper`, `beyond_time`, `chapter_complete_12`, `memory_keeper`, `ai_assist_first`, `ai_assist_10`, `guardian_aid_first`, `explore_10`, `all_explorations`, `emergency_gambit`, `survived_famine`

**影响**：成就面板分类筛选时，这些成就无法归类。  
**修复**：为每个成就补充 `category` 字段（可选值：`milestone`, `ending`, `guardian`, `playthrough`, `collection`, `vault`, `challenge`, `meta`）。  
**示例**：  
```json
{ "id": "explore_10", "category": "explore" }
{ "id": "all_explorations", "category": "explore" }
{ "id": "emergency_gambit", "category": "challenge" }
{ "id": "survived_famine", "category": "challenge" }
{ "id": "reincarnator", "category": "playthrough" }
{ "id": "eternal_keeper", "category": "playthrough" }
{ "id": "beyond_time", "category": "playthrough" }
{ "id": "memory_keeper", "category": "collection" }
```

**验证**：`python3 -c "import json; d=json.load(open('data/achievements.json')); print(sum(1 for a in d['achievements'] if not a.get('category')))"` 应返回0。

---

### T4-3：16 个结局缺 `condition.description`

**现象**：`data/endings.json` 中16个结局的 `condition` 对象缺少 `description` 字段（用于图鉴显示达成条件）。  
**影响**：结局图鉴中这些结局的达成条件显示为空。  
**修复**：为每个结局的 `condition` 添加 `description` 字符串（简短描述，如 `"在第 48 周前完成全部守护者的亲密结局"`）。  
**验证**：`python3 -c "import json; d=json.load(open('data/endings.json')); print(sum(1 for e in d['endings'] if not (e.get('condition') or {}).get('description')))"` 应返回0。

---

### T4-4：3 个叙事 flag 无消费端

**现象**：`js/game-ending.js` 在特定结局时 push 以下 flag：  
- `ghost_vision_available`（line 473）  
- `loop_crack_available`（line 479）  
- `true_ending_clue`（line 485）  

但全库搜索发现**没有任何代码读取/消费这些 flag**。  
**影响**：flag 被写入 `state.narrativeFlags` 但永不读取，是死数据。  
**修复**（二选一）：  
A. 为这些 flag 添加消费逻辑（如真结局路线中检查 `true_ending_clue` 解锁隐藏对话）  
B. 删除这些 flag 的 push 代码（如果叙事设计已变更）  

**验证**：确认每个 `narrativeFlags.push` 都有对应的 `narrativeFlags.includes` 消费点。

---

### T4-5：`desktop-build` 停 v0.2.2、`pack_release.py` 停 v0.2.1

**现象**：`desktop-build/neutralino.config.json` 中版本号可能停留在旧版本。  
`scripts/pack_release.py` 中的版本号可能也过期。  
**影响**：桌面打包产物版本号与实际不符。  
**修复**：将 `desktop-build/neutralino.config.json` 的 `version` 更新为 `0.2.4`，`pack_release.py` 中的版本常量同步更新。  
**验证**：打包后 `MemorySanctuary.exe` 的属性中版本号应为 `0.2.4`。

---

## 📋 执行顺序建议

1. **先做数据引用修复**（T2-1 ~ T2-5）—— 影响面小、无回归风险  
2. **再做逻辑账目修复**（T1-1 ~ T1-7）—— 需跑 `balance-sim-v2.js` 复核  
3. **UI/主题修复**（T3-1 ~ T3-6）—— 需浏览器双主题验证  
4. **文案/版本修复**（T4-1 ~ T4-5）—— 纯文本改动  

---

## ⚠️ 红线提醒（执行时必须遵守）

1. **零构建原生**：禁止引入 NPM/TS/Webpack  
2. **数据驱动**：业务数据只进 `data/*.json`，JS 中不硬编码  
3. **主题双适配**：所有 CSS 颜色走 `var(--...)`，dark/light 双适配  
4. **null 守卫**：所有 `init*` 函数和访问 `MemorySanctuary.state` 的函数首行 `if (!MemorySanctuary.state) return;`  
5. **MAX_WEEK=48 不可改**  
6. **改后验证**：`node -c js/*.js` 全过 + `data/*.json` parse 合法  
7. **数值改动**：必须跑 `balance-sim-v2.js` 复核归档率基线

---

*本清单由开发助手基于 `开发日志2.md` P2 backlog + 代码实际验证生成*  
*最后更新：2026-09-02*
